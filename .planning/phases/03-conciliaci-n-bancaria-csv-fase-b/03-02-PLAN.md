---
phase: 03-conciliaci-n-bancaria-csv-fase-b
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/Services/Accounting/ImportBankStatement.php
  - tests/Feature/ImportBankStatementTest.php
autonomous: true
requirements: [BANKREC-01, BANKREC-02, BANKREC-03, BANKREC-06]

must_haves:
  truths:
    - "El importador rechaza el archivo completo (sin crear ningún registro) cuando faltan encabezados reconocibles para el perfil de banco seleccionado (D-03)."
    - "El importador rechaza el archivo completo cuando el rango de fechas se solapa con un import previo de la misma CashAccount (D-04/D-05), sin merge parcial."
    - "El importador convierte Windows-1252 a UTF-8 automáticamente y detecta el delimitador (',' o ';') sin configuración manual (BANKREC-02)."
    - "Una fila con fecha o monto no interpretable queda como línea rechazada explícita con motivo, sin abortar el resto del archivo (BANKREC-06)."
  artifacts:
    - path: "app/Services/Accounting/ImportBankStatement.php"
      provides: "Servicio de dominio handle(Company, CashAccount, BankProfile, string $path): BankStatementImport"
      contains: "class ImportBankStatement"
    - path: "tests/Feature/ImportBankStatementTest.php"
      provides: "Cobertura Pest de los 4 truths anteriores"
  key_links:
    - from: "app/Services/Accounting/ImportBankStatement.php"
      to: "app/Models/BankStatementImport.php"
      via: "creación transaccional (DB::transaction) de import + líneas antes de confirmar"
      pattern: "DB::transaction"
    - from: "app/Services/Accounting/ImportBankStatement.php"
      to: "League\\Csv\\Info / League\\Csv\\CharsetConverter"
      via: "detección de delimitador y conversión de encoding ya vendorizadas (sin nueva dependencia)"
      pattern: "League\\\\Csv\\\\(Info|CharsetConverter)"
---

<objective>
Construir `ImportBankStatement`, el servicio de dominio que parsea un CSV de extracto bancario (detectando encoding/delimitador), valida encabezados y período, y crea `BankStatementImport` + `BankStatementLine` de forma transaccional — rechazando el archivo completo en los casos D-03/D-05, y filas individuales en el resto de casos (BANKREC-06), nunca en silencio.

Purpose: Es el único punto de entrada de datos de Fase B; todo lo que UI (Plan 04) y el motor de cruce (Plan 03) consumen depende de que este servicio produzca líneas correctas y filas rechazadas trazables.
Output: `app/Services/Accounting/ImportBankStatement.php` con test Pest cubriendo los 4 truths (RED→GREEN por cada comportamiento).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-conciliaci-n-bancaria-csv-fase-b/03-CONTEXT.md
@.planning/phases/03-conciliaci-n-bancaria-csv-fase-b/03-RESEARCH.md

app/Services/Imports/ArchiveMasterPreviewImporter.php
app/Enums/BankProfile.php
app/Models/BankStatementImport.php
app/Models/BankStatementLine.php
</context>

<interfaces>
<!-- Contratos creados en 03-01, consumidos aquí. No re-derivar. -->

`BankProfile::columnAliases(): array<string, array<int,string>>` (claves date/description/amount/reference), `BankProfile::dateFormats(): array<int,string>`, `BankProfile::requiredFields(): array<int,string>` — app/Enums/BankProfile.php (Plan 03-01).

`BankStatementImport` fillable: company_id, cash_account_id, bank, file_name, starts_on, ends_on, imported_by. Relación `lines(): HasMany`.

`BankStatementLine` fillable: bank_statement_import_id, line_date, description, amount, reference, raw_row, status, reject_reason. `status` casts a `BankStatementLineStatus` (Pending/Matched/Rejected).

Patrón de referencia (`app/Services/Imports/ArchiveMasterPreviewImporter.php`): `DB::beginTransaction()`/`DB::commit()`/`DB::rollBack()` envolviendo toda la operación; `parseDate()` usa `CarbonImmutable::hasFormat($date, $format)` ANTES de `createFromFormat()` (nunca confiar en el parseo "lenient" de Carbon/DateTime).

Mecanismo verificado en `vendor/filament/actions/src/ImportAction.php:417-494` (ver 03-RESEARCH.md Pattern 1) para encoding/delimitador — replicar exactamente ese orden (UTF-8 primero, luego Windows-1252, luego ISO-8859-1; ver Pitfall 2 de 03-RESEARCH.md).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encoding/delimitador, validación de encabezados (D-03) y guardia de período solapado (D-04/D-05)</name>
  <files>app/Services/Accounting/ImportBankStatement.php, tests/Feature/ImportBankStatementTest.php</files>
  <read_first>
    - app/Services/Imports/ArchiveMasterPreviewImporter.php (patrón transaccional + convención de nombres)
    - app/Enums/BankProfile.php (columnAliases/dateFormats/requiredFields ya definidos en Plan 03-01)
    - .planning/phases/03-conciliaci-n-bancaria-csv-fase-b/03-RESEARCH.md sección "Motor de cruce por lote" NO aplica aquí, pero sí "Pattern 1" y "Código de solape de período" — copiar el algoritmo tal cual.
  </read_first>
  <behavior>
    - Test 1: dado un CSV UTF-8 válido con encabezados "Fecha,Descripción,Valor,Referencia" (delimitador `,`) y perfil Bancolombia, `handle()` retorna un `BankStatementImport` persistido con `starts_on`/`ends_on` iguales al mínimo/máximo de fecha de las filas válidas.
    - Test 2: el mismo CSV pero con delimitador `;` (encabezados "Fecha;Descripción;Valor;Referencia") también se parsea correctamente (auto-detección de delimitador, D-03).
    - Test 3: un CSV cuyo encabezado no contiene ningún alias de "amount" (ej. solo "Fecha,Descripción") lanza `ValidationException` con clave `file` y el mensaje exacto: "El archivo no tiene encabezados de columna reconocibles. Verifica que sea un CSV exportado del banco seleccionado y vuelve a intentarlo." — y `BankStatementImport::count()` no cambia.
    - Test 4: importar un segundo archivo cuyo rango de fechas se solapa (aunque sea un solo día) con un `BankStatementImport` ya existente para la misma `cash_account_id` lanza `ValidationException` con clave `file` conteniendo "se solapa" — y ni el import ni ninguna línea nueva se crean (verificar conteos antes/después sin cambio).
    - Test 5: el mismo archivo importado para una `CashAccount` DIFERENTE (mismo rango de fechas) SÍ se importa correctamente (el solape es por cuenta, no global).
  </behavior>
  <action>
Crear `app/Services/Accounting/ImportBankStatement.php` con:

```php
public function handle(Company $company, CashAccount $cashAccount, BankProfile $bank, string $absolutePath): BankStatementImport
```

Implementar en este orden (RED primero con Tests 1-5, luego GREEN):
1. Abrir el archivo con `fopen($absolutePath, 'r')`, leer una muestra de 8192 bytes, detectar encoding probando `mb_check_encoding()` en el orden `['UTF-8', 'Windows-1252', 'ISO-8859-1']` (UTF-8 SIEMPRE primero — Pitfall 2 de RESEARCH). Si no es UTF-8, registrar `CharsetConverter::register()` y aplicar `stream_filter_append` con `CharsetConverter::getFiltername($encoding, 'UTF-8')`.
2. Crear `League\Csv\Reader::from($resource)` sin `setHeaderOffset` todavía; usar `League\Csv\Info::getDelimiterStats($csv, [',', ';'], 10)` y tomar el delimitador con mayor conteo; `$csv->setDelimiter($delimiter)`; `$csv->setHeaderOffset(0)`.
3. Resolver el header real del CSV contra `$bank->columnAliases()`: para cada campo en `$bank->requiredFields()` (date/description/amount), buscar (case-insensitive, trim) algún alias presente en `$csv->getHeader()`. Si falta cualquiera, lanzar `ValidationException::withMessages(['file' => 'El archivo no tiene encabezados de columna reconocibles. Verifica que sea un CSV exportado del banco seleccionado y vuelve a intentarlo.'])` — sin crear ningún registro (esto ocurre ANTES de cualquier escritura a BD). `reference` se resuelve igual pero es opcional (null si no hay alias presente).
4. Iterar `$csv->getRecords()` una primera vez SOLO para detectar `starts_on`/`ends_on`: por cada fila, intentar parsear la columna de fecha resuelta contra `$bank->dateFormats()` con `CarbonImmutable::hasFormat()` + `createFromFormat()` (patrón exacto de `ArchiveMasterPreviewImporter::parseDate()`); acumular solo las fechas que sí parsean. Si CERO filas tienen fecha válida, lanzar `ValidationException::withMessages(['file' => 'No se pudo interpretar ninguna fila del archivo. Verifica el formato y el banco seleccionado.'])`.
5. ANTES de crear cualquier registro, ejecutar la guardia de solape (D-04/D-05) exactamente como en 03-RESEARCH.md sección "Código de solape de período": `BankStatementImport::query()->where('cash_account_id', $cashAccount->id)->where(fn ($q) => $q->whereBetween('starts_on', [$startsOn, $endsOn])->orWhereBetween('ends_on', [$startsOn, $endsOn])->orWhere(fn ($q) => $q->where('starts_on', '<=', $startsOn)->where('ends_on', '>=', $endsOn)))->exists()`. Si true, lanzar `ValidationException::withMessages(['file' => "Este archivo se solapa con un extracto ya importado para esta cuenta (rango {$startsOn}–{$endsOn}). Recorta el archivo para cubrir solo fechas nuevas y vuelve a intentarlo."])`.
6. Solo después de pasar las 5 validaciones anteriores, envolver en `DB::transaction()` la creación del `BankStatementImport` (company_id, cash_account_id, bank=>$bank, file_name=>basename($absolutePath), starts_on, ends_on, imported_by=>auth()->id()). Dejar la creación de líneas para Task 2 (por ahora, Task 1 puede crear el import sin líneas para que los Tests 1-5 pasen sobre el conteo de imports/validaciones; Task 2 completa las líneas).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ImportBankStatementTest</automated>
  </verify>
  <acceptance_criteria>
    - Los 5 tests de este task pasan (`php artisan test --compact --filter=ImportBankStatementTest` sin fallos)
    - `grep -q "mb_check_encoding" app/Services/Accounting/ImportBankStatement.php`
    - `grep -q "Info::getDelimiterStats" app/Services/Accounting/ImportBankStatement.php`
    - `grep -q "CharsetConverter" app/Services/Accounting/ImportBankStatement.php`
    - El orden de candidatos de encoding en el código es literalmente `['UTF-8', 'Windows-1252', 'ISO-8859-1']` (UTF-8 primero)
  </acceptance_criteria>
  <done>El servicio detecta encoding/delimitador, valida encabezados y rechaza períodos solapados por CashAccount, sin crear registros en los casos de rechazo total.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Creación de líneas, normalización de monto/fecha y rechazo fila-por-fila (BANKREC-02/06)</name>
  <files>app/Services/Accounting/ImportBankStatement.php, tests/Feature/ImportBankStatementTest.php</files>
  <read_first>
    - app/Services/Imports/ArchiveMasterPreviewImporter.php (patrón `rejected: array<{reason}>` sin abortar el resto)
  </read_first>
  <behavior>
    - Test 6: un archivo con 3 filas válidas y 1 fila con fecha "31/13/2026" (mes inválido, no reinterpretable) produce un import con 3 `BankStatementLine` status=pending y 1 con status=rejected + `reject_reason` conteniendo "fecha" — el import se crea igual (no se aborta el archivo completo).
    - Test 7: una fila con columna de monto vacía o no numérica (ej. "N/D") produce una línea rejected con `reject_reason` conteniendo "monto" o "valor", sin afectar las demás filas.
    - Test 8: un archivo generado en Windows-1252 con una descripción que contiene "Depósito" (con tilde) se importa y la `BankStatementLine.description` resultante contiene "Depósito" en UTF-8 correcto (sin mojibake) — construir el fixture con `mb_convert_encoding('...Depósito...', 'Windows-1252', 'UTF-8')` antes de escribir el archivo de prueba.
    - Test 9: el monto de una fila con formato "150.000,50" (separador de miles `.`, decimal `,`) se normaliza a `150000.50` en `BankStatementLine.amount`.
    - Test 10: `raw_row` de cada `BankStatementLine` (incluidas las rechazadas) contiene el array crudo original de la fila del CSV (trazabilidad — Core Value).
  </behavior>
  <action>
Extender `ImportBankStatement::handle()` (dentro de la misma transacción del Task 1, después de pasar las validaciones de archivo completo):

1. Segunda iteración de `$csv->getRecords()` (o reutilizar las filas ya leídas en memoria como array desde el Task 1 — evitar leer el stream dos veces si el Reader ya fue consumido; usar `iterator_to_array($csv->getRecords())` una sola vez y operar sobre el array en ambos pasos).
2. Por cada fila: resolver `date_raw`, `description_raw`, `amount_raw`, `reference_raw` según las columnas mapeadas en Task 1.
3. Parsear fecha con el mismo mecanismo de `CarbonImmutable::hasFormat()`/`createFromFormat()`; si falla, crear `BankStatementLine` con `status => BankStatementLineStatus::Rejected`, `reject_reason => "No se pudo interpretar la fecha [{$dateRaw}]."`, `raw_row => $record`, `line_date => null`, `amount => null`, y `continue` (no abortar el resto).
4. Si la fecha parsea, normalizar el monto con un método privado `normalizeAmount(string $raw): ?float`:
   ```php
   private function normalizeAmount(string $raw): ?float
   {
       $trimmed = trim($raw);
       if ($trimmed === '') {
           return null;
       }
       $clean = str_replace(['$', ' ', '+'], '', $trimmed);
       if (str_contains($clean, ',')) {
           $clean = str_replace('.', '', $clean);
           $clean = str_replace(',', '.', $clean);
       }
       return is_numeric($clean) ? (float) $clean : null;
   }
   ```
   Si retorna `null`, crear línea `status => Rejected`, `reject_reason => "El valor de la columna monto no es numérico: [{$amountRaw}]."`, `raw_row => $record`, y `continue`.
5. Si fecha y monto parsean, crear `BankStatementLine` con `status => BankStatementLineStatus::Pending`, `line_date`, `description => $descriptionRaw`, `amount => $normalizedAmount`, `reference => $referenceRaw ?: null`, `raw_row => $record`.
6. Todas las creaciones de línea ocurren dentro del mismo `DB::transaction()` que crea el `BankStatementImport` — si algo lanza una excepción no controlada, todo se revierte (atomicidad).
7. Retornar `$import->load('lines')`.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=ImportBankStatementTest</automated>
  </verify>
  <acceptance_criteria>
    - Los 10 tests del archivo (Task 1 + Task 2) pasan
    - `grep -q "normalizeAmount" app/Services/Accounting/ImportBankStatement.php`
    - `grep -q "BankStatementLineStatus::Rejected" app/Services/Accounting/ImportBankStatement.php`
    - `grep -q "raw_row" app/Services/Accounting/ImportBankStatement.php` (toda fila, válida o rechazada, persiste su dato crudo)
    - `vendor/bin/pint --dirty --format agent` sin cambios pendientes sobre `ImportBankStatement.php`
  </acceptance_criteria>
  <done>`ImportBankStatement::handle()` crea líneas pending/rejected fila-por-fila, normaliza monto/fecha/encoding, nunca omite una fila en silencio, y el import completo es atómico.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=ImportBankStatementTest` — 10 tests, todos en verde.
- `vendor/bin/pint --dirty --format agent` limpio.
- Ningún archivo fuera de `files_modified` fue tocado.
</verification>

<success_criteria>
BANKREC-01 (asociación a CashAccount), BANKREC-02 (encoding/fecha), BANKREC-03 (dedup por período) y BANKREC-06 (rechazo fila explícito) quedan implementados y probados end-to-end contra CSVs reales construidos en los fixtures del test, sin ninguna dependencia nueva de Composer.
</success_criteria>

<output>
After completion, create `.planning/phases/03-conciliaci-n-bancaria-csv-fase-b/03-02-SUMMARY.md`
</output>
