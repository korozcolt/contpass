---
phase: 01-cotizaci-n-electr-nica-fase-a
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - app/Services/Accounting/BuildQuotationNumber.php
  - tests/Feature/QuotationNumberingTest.php
  - app/Services/Accounting/ConvertQuotationToIncome.php
  - tests/Feature/QuotationConversionTest.php
autonomous: true
requirements: [QUOT-02, QUOT-05, QUOT-06, QUOT-07]

must_haves:
  truths:
    - "La numeración de cotizaciones es consecutiva por empresa (COT-AAAA-NNNNN) y nunca se repite entre dos empresas ni dentro de la misma empresa"
    - "Convertir una cotización Aceptada crea un IncomeRecord reusando PostIncomeVoucher, en una sola acción de un clic"
    - "Convertir la misma cotización dos veces nunca crea un segundo comprobante — la segunda conversión falla explícitamente"
  artifacts:
    - path: "app/Services/Accounting/BuildQuotationNumber.php"
      provides: "numeración COT-{año}-{5 dígitos}, company-scoped, con lockForUpdate() dentro de DB::transaction()"
    - path: "app/Services/Accounting/ConvertQuotationToIncome.php"
      provides: "conversión idempotente Quotation Aceptada -> Voucher, guard de estado+voucher_id bajo lockForUpdate()"
  key_links:
    - from: "app/Services/Accounting/ConvertQuotationToIncome.php"
      to: "app/Services/Accounting/PostIncomeVoucher.php"
      via: "constructor injection + ->handle(Company, ThirdParty, array) sin modificar su firma"
      pattern: "PostIncomeVoucher"
    - from: "app/Services/Accounting/ConvertQuotationToIncome.php"
      to: "app/Models/Quotation.php"
      via: "Quotation::query()->whereKey(...)->lockForUpdate()->firstOrFail() dentro de DB::transaction()"
      pattern: "lockForUpdate"
    - from: "app/Services/Accounting/BuildQuotationNumber.php"
      to: "app/Models/Quotation.php"
      via: "conteo company-scoped con lockForUpdate() sobre Quotation::query()"
      pattern: "lockForUpdate"
---

<objective>
Construir los dos servicios de dominio que corrigen los dos bugs ya diagnosticados en el código existente (numeración no company-scoped/no locked en `BuildVoucherNumber`, y ausencia total de guard de idempotencia en `PostIncomeVoucher`): `BuildQuotationNumber` (numeración segura ante concurrencia) y `ConvertQuotationToIncome` (conversión idempotente que reusa `PostIncomeVoucher` sin tocarlo).

Purpose: QUOT-02 (numeración sin duplicados) y QUOT-05/06/07 (conversión de un clic, idempotente, con enlace al comprobante) dependen enteramente de estos dos servicios — son la lógica de negocio real de la fase, el resto (Filament, PDF) es solo presentación sobre ellos.
Output: `App\Services\Accounting\BuildQuotationNumber`, `App\Services\Accounting\ConvertQuotationToIncome`, y dos suites Pest que fijan su comportamiento observable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-CONTEXT.md
@.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-RESEARCH.md

<interfaces>
<!-- Contratos exactos de los que dependen estos dos servicios — no releer el código fuente, usar directamente. -->

From `app/Models/Quotation.php` (Plan 1, ya implementado):
```php
class Quotation extends Model
{
    // fillable: company_id, third_party_id, number, status, revenue_account_id,
    //           receivable_account_id, notes, expires_on, rejection_reason, voucher_id
    public function company(): BelongsTo;       // -> Company
    public function thirdParty(): BelongsTo;    // -> ThirdParty
    public function lines(): HasMany;           // -> QuotationLine
    public function voucher(): BelongsTo;       // -> Voucher
    public function getTotalAttribute(): float; // suma de lines->subtotal
    public function isReadOnly(): bool;         // true solo si status === Converted
    // status casts a App\Enums\QuotationStatus
}
```

From `app/Services/Accounting/PostIncomeVoucher.php` (existente, NO modificar — firma exacta):
```php
namespace App\Services\Accounting;

class PostIncomeVoucher
{
    public function __construct(private readonly PostsBalancedVoucher $postsBalancedVoucher) {}

    /**
     * @param  array{third_party_id: int, revenue_account_id: int, receivable_account_id: int, support_number: string, accrual_date: string, amount: numeric-string|float|int, description?: string}  $data
     */
    public function handle(Company $company, ThirdParty $thirdParty, array $data): Voucher;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BuildQuotationNumber (numeración company-scoped, concurrency-safe)</name>
  <files>app/Services/Accounting/BuildQuotationNumber.php, tests/Feature/QuotationNumberingTest.php</files>
  <read_first>
    app/Services/Accounting/BuildVoucherNumber.php
    app/Models/Quotation.php
    app/Models/Company.php
  </read_first>
  <behavior>
    - Test 1: en una empresa sin cotizaciones previas este año, `next($company)` retorna `"COT-{año actual}-00001"`.
    - Test 2: tras crear una `Quotation` con `number` generado por `next($company)`, una segunda llamada a `next($company)` retorna `"COT-{año actual}-00002"`.
    - Test 3: dos empresas distintas, cada una sin cotizaciones previas, obtienen ambas `"COT-{año actual}-00001"` de forma independiente (scoping por `company_id`).
    - Test 4: una `Quotation` existente con `number` de un año anterior (ej. `COT-2025-00099`) NO afecta el conteo del año actual — `next($company)` sigue retornando `00001` si no hay cotizaciones del año actual.
  </behavior>
  <action>
    **NO copiar el patrón de `BuildVoucherNumber`** (bug conocido: `Voucher::query()->where('type', ...)->count()` no está scoped por `company_id` ni usa lock — ver Pitfall 2 de RESEARCH.md). Implementación exacta:

    ```php
    namespace App\Services\Accounting;

    use App\Models\Company;
    use App\Models\Quotation;
    use Illuminate\Support\Facades\DB;

    class BuildQuotationNumber
    {
        public function next(Company $company): string
        {
            return DB::transaction(function () use ($company): string {
                $year = now()->format('Y');

                $count = Quotation::query()
                    ->whereBelongsTo($company)
                    ->where('number', 'like', "COT-{$year}-%")
                    ->lockForUpdate()
                    ->count();

                return sprintf('COT-%s-%05d', $year, $count + 1);
            });
        }
    }
    ```

    Formato exacto por D-03: prefijo `COT-`, año de 4 dígitos, guion, consecutivo de **5 dígitos** con ceros a la izquierda (`%05d` — nótese que difiere de los 6 dígitos de `BuildVoucherNumber`, no copiar ese formato).

    Escribe primero el test con los 4 casos de `<behavior>`, confírmalo en rojo (clase no existe), luego implementa.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=QuotationNumberingTest</automated>
  </verify>
  <done>`BuildQuotationNumber::next()` produce números `COT-AAAA-NNNNN` company-scoped y year-scoped, envuelto en `DB::transaction()` + `lockForUpdate()`; los 4 casos de `<behavior>` pasan.</done>
  <acceptance_criteria>
    - `php artisan test --compact --filter=QuotationNumberingTest` sale con código 0
    - `app/Services/Accounting/BuildQuotationNumber.php` contiene `lockForUpdate()` y `DB::transaction(`
    - `app/Services/Accounting/BuildQuotationNumber.php` contiene `whereBelongsTo($company)` (scoping por empresa)
    - `app/Services/Accounting/BuildQuotationNumber.php` contiene `sprintf('COT-%s-%05d'`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConvertQuotationToIncome (conversión idempotente reusando PostIncomeVoucher)</name>
  <files>app/Services/Accounting/ConvertQuotationToIncome.php, tests/Feature/QuotationConversionTest.php</files>
  <read_first>
    app/Services/Accounting/PostIncomeVoucher.php
    app/Filament/Resources/IncomeRecords/Pages/CreateIncomeRecord.php
    app/Models/Quotation.php
    app/Enums/QuotationStatus.php
  </read_first>
  <behavior>
    - Test 1: `handle()` sobre una `Quotation` con `status = Accepted` y `voucher_id = null` crea exactamente un `Voucher` (con su `IncomeRecord`), y deja la cotización con `status = Converted` y `voucher_id` apuntando al voucher creado. El monto del voucher (`incomeRecord.amount`) es igual a `$quotation->total`.
    - Test 2: llamar `handle()` una segunda vez sobre esa misma cotización (ya `Converted`) lanza `ValidationException` con el mensaje `'Esta cotización ya fue convertida o no está Aceptada.'` en la clave `status`, y el número de `Voucher` en la base de datos no aumenta respecto al estado tras la primera conversión.
    - Test 3: `handle()` sobre una `Quotation` con `status = Draft` (nunca aceptada) lanza la misma `ValidationException` sin invocar `PostIncomeVoucher` (ningún `Voucher` se crea).
  </behavior>
  <action>
    Adapta el patrón ya usado en `CreateIncomeRecord::handleRecordCreation()` (resolver relación, invocar el servicio existente, retornar resultado) pero envolviéndolo en el guard de idempotencia que `PostIncomeVoucher` NO tiene (Pitfall 3 de RESEARCH.md: `PostIncomeVoucher` es genérico y no sabe si ya fue "convertida" — el guard vive aquí, no ahí). El re-fetch con `lockForUpdate()` DENTRO de la transacción es lo que cierra la carrera de doble clic — un chequeo `if ($quotation->status !== Accepted)` ANTES de abrir la transacción NO es suficiente.

    ```php
    namespace App\Services\Accounting;

    use App\Enums\QuotationStatus;
    use App\Models\Quotation;
    use App\Models\Voucher;
    use Illuminate\Support\Facades\DB;
    use Illuminate\Validation\ValidationException;

    class ConvertQuotationToIncome
    {
        public function __construct(private readonly PostIncomeVoucher $postIncomeVoucher) {}

        public function handle(Quotation $quotation): Voucher
        {
            return DB::transaction(function () use ($quotation): Voucher {
                $locked = Quotation::query()->whereKey($quotation->id)->lockForUpdate()->firstOrFail();

                if ($locked->status !== QuotationStatus::Accepted || $locked->voucher_id !== null) {
                    throw ValidationException::withMessages([
                        'status' => 'Esta cotización ya fue convertida o no está Aceptada.',
                    ]);
                }

                $voucher = $this->postIncomeVoucher->handle($locked->company, $locked->thirdParty, [
                    'third_party_id' => $locked->third_party_id,
                    'revenue_account_id' => $locked->revenue_account_id,
                    'receivable_account_id' => $locked->receivable_account_id,
                    'support_number' => $locked->number,
                    'accrual_date' => now()->toDateString(),
                    'amount' => $locked->total,
                    'description' => "Conversión de cotización {$locked->number}",
                ]);

                $locked->forceFill([
                    'voucher_id' => $voucher->id,
                    'status' => QuotationStatus::Converted,
                ])->save();

                return $voucher;
            });
        }
    }
    ```

    No pedir `revenue_account_id`/`receivable_account_id` como parámetros — ya están en `Quotation` por D-05 (se eligen en el formulario, no al convertir), por eso `handle()` solo recibe la `Quotation`.

    Escribe primero el test con los 3 casos de `<behavior>` (usa el fixture de `accountingFixture()`-style visto en `tests/Feature/AccountingPostingTest.php`, adaptado a `Quotation`/`QuotationLine`), confírmalo en rojo, luego implementa.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=QuotationConversionTest</automated>
  </verify>
  <done>`ConvertQuotationToIncome::handle()` es idempotente (guard de estado+voucher_id bajo `lockForUpdate()` en una sola transacción), reusa `PostIncomeVoucher::handle()` sin modificarlo, y los 3 casos de `<behavior>` pasan.</done>
  <acceptance_criteria>
    - `php artisan test --compact --filter=QuotationConversionTest` sale con código 0
    - `app/Services/Accounting/ConvertQuotationToIncome.php` contiene `lockForUpdate()` dentro de un `DB::transaction(`
    - `app/Services/Accounting/ConvertQuotationToIncome.php` contiene `'Esta cotización ya fue convertida o no está Aceptada.'`
    - `app/Services/Accounting/PostIncomeVoucher.php` permanece sin modificar (`git diff --stat app/Services/Accounting/PostIncomeVoucher.php` no reporta cambios)
  </acceptance_criteria>
</task>

</tasks>

<verification>
`php artisan test --compact --filter=QuotationNumberingTest` y `php artisan test --compact --filter=QuotationConversionTest` pasan ambos. `vendor/bin/pint --dirty --format agent` limpio.
</verification>

<success_criteria>
Los dos servicios de dominio de la fase existen, están probados, y el Plan 3 (recurso Filament) puede invocarlos vía `app(BuildQuotationNumber::class)->next($company)` y `app(ConvertQuotationToIncome::class)->handle($quotation)` sin ninguna lógica de negocio adicional en la capa de presentación.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-02-SUMMARY.md`
</output>
