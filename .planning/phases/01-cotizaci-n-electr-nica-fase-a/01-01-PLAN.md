---
phase: 01-cotizaci-n-electr-nica-fase-a
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/*_create_quotations_table.php
  - database/migrations/*_create_quotation_lines_table.php
  - app/Enums/QuotationStatus.php
  - app/Models/Quotation.php
  - app/Models/QuotationLine.php
  - database/factories/QuotationFactory.php
  - database/factories/QuotationLineFactory.php
  - tests/Feature/QuotationModelTest.php
autonomous: true
requirements: [QUOT-01, QUOT-03]

must_haves:
  truths:
    - "Usuario puede crear una cotización con líneas y ver el total calculado automáticamente a partir de las líneas"
    - "El estado de una cotización Enviada cuya fecha de vigencia ya pasó se calcula automáticamente como Vencida, sin ninguna transición manual"
    - "Una cotización con estado Convertida queda marcada como de solo lectura a nivel de modelo"
  artifacts:
    - path: "database/migrations/*_create_quotations_table.php"
      provides: "tabla quotations con company_id, third_party_id, number único por empresa (índice único compuesto company_id+number, no global), status, revenue_account_id, receivable_account_id, notes, expires_on, rejection_reason, voucher_id nullable único"
    - path: "database/migrations/*_create_quotation_lines_table.php"
      provides: "tabla quotation_lines con quotation_id, description, quantity, unit_price, subtotal"
    - path: "app/Enums/QuotationStatus.php"
      provides: "enum QuotationStatus (Draft/Sent/Accepted/Rejected/Converted/Expired) implements HasColor,HasIcon,HasLabel"
    - path: "app/Models/Quotation.php"
      provides: "modelo Quotation con total(), effectiveStatus(), isReadOnly(), relaciones lines/company/thirdParty/voucher/revenueAccount/receivableAccount"
      min_lines: 50
    - path: "app/Models/QuotationLine.php"
      provides: "modelo QuotationLine con subtotal auto-calculado en el evento saving"
  key_links:
    - from: "app/Models/QuotationLine.php"
      to: "app/Models/Quotation.php"
      via: "belongsTo(Quotation::class) / hasMany(QuotationLine::class)"
      pattern: "belongsTo\\(Quotation::class\\)"
    - from: "app/Models/Quotation.php"
      to: "app/Enums/QuotationStatus.php"
      via: "cast de status a QuotationStatus::class"
      pattern: "QuotationStatus::class"
---

<objective>
Construir la base de datos y el modelo de dominio de `Quotation`/`QuotationLine`: tablas nuevas, enum de estado con "Vencida" calculada (nunca manual), y modelos con los accessors que el resto de la fase (numeración, conversión, formulario Filament, PDF) va a consumir directamente.

Purpose: Establecer el contrato de datos primero (Interface-First) para que los planes siguientes (numeración+conversión, recurso Filament, PDF) construyan contra un modelo ya probado, sin re-descubrir su forma.
Output: Migraciones `quotations`/`quotation_lines`, `App\Enums\QuotationStatus`, `App\Models\Quotation`, `App\Models\QuotationLine`, factories, y un test Pest que fija el comportamiento de `total()`, `effectiveStatus()` e `isReadOnly()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-CONTEXT.md
@.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-RESEARCH.md

<interfaces>
<!-- Patrones exactos a replicar, leídos directamente del código existente. -->

From `app/Models/Voucher.php` (patrón de modelo con Auditable + casts + relaciones + método de dominio):
```php
class Voucher extends Model
{
    use Auditable, HasFactory;

    protected $fillable = [ /* ... */ ];

    protected function casts(): array
    {
        return [
            'status' => VoucherStatus::class,
            'date' => 'date',
        ];
    }

    public function ensureEditable(): void
    {
        if ($this->status !== VoucherStatus::Draft) {
            throw new RuntimeException('...');
        }
    }
}
```

From `app/Enums/VoucherStatus.php` (patrón exacto de enum a replicar para `QuotationStatus`):
```php
enum VoucherStatus: string implements HasColor, HasIcon, HasLabel
{
    case Draft = 'draft';
    // ...
    public function getLabel(): string { return $this->label(); }
    public function label(): string { return match ($this) { self::Draft => 'Borrador', /* ... */ }; }
    public function getColor(): string { return match ($this) { self::Draft => 'gray', /* ... */ }; }
    public function getIcon(): Heroicon { return match ($this) { self::Draft => Heroicon::PencilSquare, /* ... */ }; }
}
```

From `app/Models/AccountingEntry.php` y `app/Models/WarehouseMovementLine.php` (patrón de línea con casts decimales, sin lógica propia — la lógica de `subtotal` en `QuotationLine` es la única desviación nueva de este patrón, ver Task 2):
```php
protected function casts(): array
{
    return ['debit' => 'decimal:2', 'credit' => 'decimal:2'];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migraciones de quotations/quotation_lines + enum QuotationStatus</name>
  <files>database/migrations/{timestamp}_create_quotations_table.php, database/migrations/{timestamp}_create_quotation_lines_table.php, app/Enums/QuotationStatus.php</files>
  <read_first>
    database/migrations/2026_07_03_175247_000007_create_vouchers_table.php
    database/migrations/2026_07_03_175247_000009_create_income_records_table.php
    database/migrations/2026_07_30_124604_create_warehouse_movement_lines_table.php
    app/Enums/VoucherStatus.php
  </read_first>
  <action>
    Ejecuta `php artisan make:migration create_quotations_table --no-interaction` y `php artisan make:migration create_quotation_lines_table --no-interaction`, luego edita los archivos generados.

    **`quotations` table (columnas exactas):**
    ```php
    Schema::create('quotations', function (Blueprint $table) {
        $table->id();
        $table->foreignId('company_id')->constrained()->cascadeOnDelete();
        $table->foreignId('third_party_id')->constrained()->restrictOnDelete();
        $table->string('number');
        $table->string('status')->default('draft');
        $table->foreignId('revenue_account_id')->constrained('chart_accounts')->restrictOnDelete();
        $table->foreignId('receivable_account_id')->constrained('chart_accounts')->restrictOnDelete();
        $table->text('notes')->nullable();
        $table->date('expires_on');
        $table->string('rejection_reason')->nullable();
        $table->foreignId('voucher_id')->nullable()->unique()->constrained('vouchers')->nullOnDelete();
        $table->timestamps();

        $table->unique(['company_id', 'number']);
        $table->index(['company_id', 'status']);
    });
    ```
    `down()`: `Schema::dropIfExists('quotations');`

    **IMPORTANTE — `number` NO lleva `->unique()` a nivel de columna.** La numeración es consecutiva *por empresa* (`BuildQuotationNumber::next($company)` en Plan 2 cuenta cotizaciones scoped por `company_id`), así que dos empresas distintas legítimamente comparten el mismo `COT-{año}-00001` el mismo año. Un `unique()` global en `number` rompería la primera cotización de la segunda empresa. La restricción correcta es el índice **compuesto** `$table->unique(['company_id', 'number'])`: único dentro de cada empresa, libre entre empresas. Este índice compuesto es también la garantía real de "sin duplicados de numeración" ante condiciones de carrera — ver la nota de diseño en `01-02-PLAN.md` Task 1 y el manejo de colisión en `01-03-PLAN.md` Task 2.

    **`quotation_lines` table (columnas exactas):**
    ```php
    Schema::create('quotation_lines', function (Blueprint $table) {
        $table->id();
        $table->foreignId('quotation_id')->constrained()->cascadeOnDelete();
        $table->string('description');
        $table->decimal('quantity', 15, 2);
        $table->decimal('unit_price', 15, 2);
        $table->decimal('subtotal', 15, 2)->default(0);
        $table->timestamps();

        $table->index(['quotation_id']);
    });
    ```
    `down()`: `Schema::dropIfExists('quotation_lines');`

    **`app/Enums/QuotationStatus.php`** — mismo patrón que `VoucherStatus` (namespace `App\Enums`, implements `HasColor, HasIcon, HasLabel`), con 6 casos. `Expired` es un caso real del enum pero NUNCA se persiste directamente en la columna `status` — solo lo retorna el accessor `effectiveStatus()` en Task 2 (ver Pitfall 4 de RESEARCH.md: nunca ofrecer una acción "Marcar como vencida").

    ```php
    enum QuotationStatus: string implements HasColor, HasIcon, HasLabel
    {
        case Draft = 'draft';
        case Sent = 'sent';
        case Accepted = 'accepted';
        case Rejected = 'rejected';
        case Converted = 'converted';
        case Expired = 'expired';

        public function getLabel(): string { return $this->label(); }

        public function label(): string
        {
            return match ($this) {
                self::Draft => 'Borrador',
                self::Sent => 'Enviada',
                self::Accepted => 'Aceptada',
                self::Rejected => 'Rechazada',
                self::Converted => 'Convertida',
                self::Expired => 'Vencida',
            };
        }

        public function getColor(): string
        {
            return match ($this) {
                self::Draft => 'gray',
                self::Sent => 'info',
                self::Accepted => 'success',
                self::Rejected => 'danger',
                self::Converted => 'primary',
                self::Expired => 'warning',
            };
        }

        public function getIcon(): Heroicon
        {
            return match ($this) {
                self::Draft => Heroicon::PencilSquare,
                self::Sent => Heroicon::PaperAirplane,
                self::Accepted => Heroicon::CheckCircle,
                self::Rejected => Heroicon::XCircle,
                self::Converted => Heroicon::CheckBadge,
                self::Expired => Heroicon::Clock,
            };
        }
    }
    ```
    Colores/iconos copiados exactamente de `.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-UI-SPEC.md` sección "Panel surface status colors" — no improvisar valores distintos.
  </action>
  <verify>
    <automated>grep -q "create('quotations'" database/migrations/*_create_quotations_table.php && grep -q "unique(\['company_id', 'number'\])" database/migrations/*_create_quotations_table.php && grep -q "create('quotation_lines'" database/migrations/*_create_quotation_lines_table.php && grep -q "implements HasColor, HasIcon, HasLabel" app/Enums/QuotationStatus.php && vendor/bin/pint --dirty --format agent</automated>
  </verify>
  <done>Ambas migraciones existen con las columnas exactas listadas arriba, `number` está protegido por un índice único COMPUESTO `company_id+number` (no un `unique()` global de columna); `QuotationStatus` tiene 6 casos con label/color/icon coherentes con UI-SPEC.</done>
  <acceptance_criteria>
    - `database/migrations/*_create_quotations_table.php` contiene las cadenas: `third_party_id`, `revenue_account_id`, `receivable_account_id`, `expires_on`, `rejection_reason`, `voucher_id`
    - `database/migrations/*_create_quotations_table.php` contiene `unique(['company_id', 'number'])` y NO contiene `string('number')->unique()`
    - `database/migrations/*_create_quotation_lines_table.php` contiene: `quotation_id`, `subtotal`
    - `app/Enums/QuotationStatus.php` contiene los 6 casos: `case Draft`, `case Sent`, `case Accepted`, `case Rejected`, `case Converted`, `case Expired`
    - `vendor/bin/pint --dirty --format agent` termina sin cambios pendientes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelos Quotation/QuotationLine + factories + comportamiento total/effectiveStatus/isReadOnly</name>
  <files>app/Models/Quotation.php, app/Models/QuotationLine.php, database/factories/QuotationFactory.php, database/factories/QuotationLineFactory.php, tests/Feature/QuotationModelTest.php</files>
  <read_first>
    app/Models/Voucher.php
    app/Models/ThirdParty.php
    app/Models/ChartAccount.php
    database/factories/VoucherFactory.php
    database/factories/ThirdPartyFactory.php
    database/factories/ChartAccountFactory.php
    app/Enums/QuotationStatus.php
  </read_first>
  <behavior>
    - Test 1: una `Quotation` con dos `QuotationLine` (2×10000 y 3×5000) expone `->total` === 35000.0 (accessor que suma `lines->subtotal`, no una columna almacenada).
    - Test 2: al guardar una `QuotationLine` con `quantity=2, unit_price=10000` sin fijar `subtotal`, el modelo calcula y persiste `subtotal=20000.00` automáticamente (evento `saving`).
    - Test 3: `effectiveStatus()` retorna `QuotationStatus::Expired` únicamente cuando `status === QuotationStatus::Sent` Y `expires_on` es una fecha pasada; una cotización `Draft` con `expires_on` pasado sigue retornando `QuotationStatus::Draft` (Draft nunca vence); una `Sent` con `expires_on` futuro retorna `QuotationStatus::Sent`.
    - Test 4: `isReadOnly()` es `true` únicamente cuando `status === QuotationStatus::Converted`; `false` para los demás 5 estados.
  </behavior>
  <action>
    **`app/Models/Quotation.php`:**
    ```php
    namespace App\Models;

    use App\Enums\QuotationStatus;
    use App\Traits\Auditable;
    use Database\Factories\QuotationFactory;
    use Illuminate\Database\Eloquent\Factories\HasFactory;
    use Illuminate\Database\Eloquent\Model;
    use Illuminate\Database\Eloquent\Relations\BelongsTo;
    use Illuminate\Database\Eloquent\Relations\HasMany;

    class Quotation extends Model
    {
        /** @use HasFactory<QuotationFactory> */
        use Auditable, HasFactory;

        protected $fillable = [
            'company_id', 'third_party_id', 'number', 'status',
            'revenue_account_id', 'receivable_account_id',
            'notes', 'expires_on', 'rejection_reason', 'voucher_id',
        ];

        protected $attributes = [
            'status' => 'draft',
        ];

        protected function casts(): array
        {
            return [
                'status' => QuotationStatus::class,
                'expires_on' => 'date',
            ];
        }

        public function company(): BelongsTo { return $this->belongsTo(Company::class); }
        public function thirdParty(): BelongsTo { return $this->belongsTo(ThirdParty::class); }
        public function revenueAccount(): BelongsTo { return $this->belongsTo(ChartAccount::class, 'revenue_account_id'); }
        public function receivableAccount(): BelongsTo { return $this->belongsTo(ChartAccount::class, 'receivable_account_id'); }
        public function voucher(): BelongsTo { return $this->belongsTo(Voucher::class); }
        public function lines(): HasMany { return $this->hasMany(QuotationLine::class); }

        public function getTotalAttribute(): float
        {
            $lines = $this->relationLoaded('lines') ? $this->lines : $this->lines()->get();

            return round((float) $lines->sum('subtotal'), 2);
        }

        public function effectiveStatus(): QuotationStatus
        {
            if ($this->status === QuotationStatus::Sent && $this->expires_on !== null && $this->expires_on->isPast()) {
                return QuotationStatus::Expired;
            }

            return $this->status;
        }

        public function isReadOnly(): bool
        {
            return $this->status === QuotationStatus::Converted;
        }
    }
    ```

    **`app/Models/QuotationLine.php`:**
    ```php
    namespace App\Models;

    use Database\Factories\QuotationLineFactory;
    use Illuminate\Database\Eloquent\Factories\HasFactory;
    use Illuminate\Database\Eloquent\Model;
    use Illuminate\Database\Eloquent\Relations\BelongsTo;

    class QuotationLine extends Model
    {
        /** @use HasFactory<QuotationLineFactory> */
        use HasFactory;

        protected $fillable = ['quotation_id', 'description', 'quantity', 'unit_price', 'subtotal'];

        protected function casts(): array
        {
            return [
                'quantity' => 'decimal:2',
                'unit_price' => 'decimal:2',
                'subtotal' => 'decimal:2',
            ];
        }

        protected static function booted(): void
        {
            static::saving(function (self $line): void {
                $line->subtotal = round((float) $line->quantity * (float) $line->unit_price, 2);
            });
        }

        public function quotation(): BelongsTo { return $this->belongsTo(Quotation::class); }
    }
    ```

    **Factories** (mismo patrón que `VoucherFactory`/`ChartAccountFactory` — `company_id`, third party y cuentas resueltas vía `Company::factory()`/`ThirdParty::factory()`/`ChartAccount::factory()`):
    ```php
    // database/factories/QuotationFactory.php
    class QuotationFactory extends Factory
    {
        public function definition(): array
        {
            $company = Company::factory();

            return [
                'company_id' => $company,
                'third_party_id' => ThirdParty::factory()->state(['company_id' => $company]),
                'number' => fake()->unique()->bothify('COT-2026-#####'),
                'status' => QuotationStatus::Draft,
                'revenue_account_id' => ChartAccount::factory()->credit()->state(['company_id' => $company, 'code' => '413595']),
                'receivable_account_id' => ChartAccount::factory()->state(['company_id' => $company, 'code' => '130505']),
                'notes' => null,
                'expires_on' => now()->addDays(30)->toDateString(),
                'rejection_reason' => null,
                'voucher_id' => null,
            ];
        }
    }

    // database/factories/QuotationLineFactory.php
    class QuotationLineFactory extends Factory
    {
        public function definition(): array
        {
            return [
                'quotation_id' => Quotation::factory(),
                'description' => fake()->words(3, true),
                'quantity' => 1,
                'unit_price' => fake()->randomFloat(2, 10000, 500000),
            ];
        }
    }
    ```

    Nota: como `number` ahora es único solo por empresa (índice compuesto, Task 1), `fake()->unique()->bothify('COT-2026-#####')` en la factory sigue siendo válido — Faker garantiza unicidad global entre llamadas dentro del mismo test run, lo cual es más estricto que el mínimo requerido y no genera colisiones falsas.

    Escribe primero `tests/Feature/QuotationModelTest.php` cubriendo los 4 comportamientos de `<behavior>`, confirma que fallan (modelos no existen aún), luego implementa los modelos/factories hasta que pasen.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=QuotationModelTest</automated>
  </verify>
  <done>Los 4 comportamientos de `<behavior>` pasan; `Quotation::total`, `effectiveStatus()`, `isReadOnly()` y el auto-cálculo de `QuotationLine::subtotal` están implementados exactamente como se especifica.</done>
  <acceptance_criteria>
    - `php artisan test --compact --filter=QuotationModelTest` sale con código 0
    - `app/Models/Quotation.php` contiene `function effectiveStatus(): QuotationStatus` y `function isReadOnly(): bool`
    - `app/Models/QuotationLine.php` contiene `static::saving(`
    - `database/factories/QuotationFactory.php` y `database/factories/QuotationLineFactory.php` existen
  </acceptance_criteria>
</task>

</tasks>

<verification>
`php artisan test --compact --filter=QuotationModelTest` pasa. `vendor/bin/pint --dirty --format agent` no reporta cambios pendientes tras los edits de este plan.
</verification>

<success_criteria>
Migraciones, enum y modelos de `Quotation`/`QuotationLine` existen, están probados, y exponen exactamente los métodos (`total`, `effectiveStatus()`, `isReadOnly()`) que los planes 2, 3 y 4 de esta fase van a consumir sin necesidad de releer este código fuente.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-01-SUMMARY.md`
</output>
