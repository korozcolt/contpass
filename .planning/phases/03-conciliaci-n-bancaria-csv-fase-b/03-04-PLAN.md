---
phase: 03-conciliaci-n-bancaria-csv-fase-b
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - app/Filament/Pages/UploadBankStatement.php
  - resources/views/filament/pages/upload-bank-statement.blade.php
  - app/Filament/Pages/BankStatementReview.php
  - resources/views/filament/pages/bank-statement-review.blade.php
  - tests/Feature/BankStatementUploadTest.php
  - tests/Feature/BankStatementReviewTest.php
autonomous: true
requirements: [BANKREC-01, BANKREC-05]

must_haves:
  truths:
    - "Usuario puede subir un CSV desde el panel Filament, asociarlo a una CashAccount y a un banco (Bancolombia/Davivienda), y ser redirigido a la revisión de ese import específico."
    - "Errores de encabezados faltantes (D-03) o período solapado (D-05) se muestran al usuario con el texto exacto de 03-UI-SPEC.md, sin crear ningún registro."
    - "Usuario ve las líneas pendientes de un import con sus candidatos de cruce, puede confirmar o descartar cada sugerencia individualmente, y ninguna se confirma sin su acción explícita (BANKREC-05)."
    - "Las filas rechazadas (BANKREC-06) son visibles en un banner persistente con el motivo de cada una, y las líneas ya conciliadas desaparecen de la lista de pendientes (empty state 'Todo conciliado' cuando no queda ninguna)."
  artifacts:
    - path: "app/Filament/Pages/UploadBankStatement.php"
      provides: "Página Filament con formulario CashAccount+BankProfile+FileUpload que invoca ImportBankStatement + ProposeBankStatementMatches"
      contains: "class UploadBankStatement extends Page"
    - path: "app/Filament/Pages/BankStatementReview.php"
      provides: "Página Filament dedicada por import (D-09) con tabla de líneas pending + acciones Confirmar/Descartar + banner de rechazadas"
      contains: "class BankStatementReview extends Page"
  key_links:
    - from: "app/Filament/Pages/UploadBankStatement.php"
      to: "app/Services/Accounting/ImportBankStatement.php"
      via: "llamada directa al servicio desde el submit handler, nunca lógica de import en el formulario"
      pattern: "ImportBankStatement::class"
    - from: "app/Filament/Pages/UploadBankStatement.php"
      to: "app/Filament/Pages/BankStatementReview.php"
      via: "redirect tras import exitoso usando BankStatementReview::getUrl(['import' => \$import->id])"
      pattern: "BankStatementReview::getUrl"
    - from: "app/Filament/Pages/BankStatementReview.php"
      to: "app/Services/Accounting/ConfirmBankStatementMatch.php"
      via: "acción de tabla 'Confirmar cruce' invoca el servicio, nunca escribe Payment directamente"
      pattern: "ConfirmBankStatementMatch::class"
---

<objective>
Construir las dos páginas Filament de Fase B (D-09): `UploadBankStatement` (subir extracto + banco + CashAccount, invoca los servicios de Plans 02/03) y `BankStatementReview` (revisar líneas pendientes de un import, confirmar/descartar cruces). Cierra BANKREC-01 (UI de subida) y BANKREC-05 (confirmación explícita en UI) de punta a punta.

Purpose: Es el único punto de contacto del usuario con Fase B — sin esto, los servicios de Plans 02/03 no son alcanzables desde el panel.
Output: `app/Filament/Pages/UploadBankStatement.php` + blade, `app/Filament/Pages/BankStatementReview.php` + blade, tests Pest/Livewire cubriendo el flujo subir→revisar→confirmar/descartar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-conciliaci-n-bancaria-csv-fase-b/03-CONTEXT.md
@.planning/phases/03-conciliaci-n-bancaria-csv-fase-b/03-UI-SPEC.md

app/Filament/Pages/CompanySettings.php
app/Filament/Pages/BankReconciliationReport.php
app/Filament/Pages/AccountabilityCenter.php
app/Providers/Filament/AdminPanelProvider.php
</context>

<interfaces>
<!-- Contratos de Plans 01/02/03, consumidos aquí. No re-derivar. -->

`ImportBankStatement::handle(Company $company, CashAccount $cashAccount, BankProfile $bank, string $absolutePath): BankStatementImport` — lanza `ValidationException` con clave `file` en los casos D-03/D-05 (Plan 03-02).

`ProposeBankStatementMatches::handle(BankStatementImport $import): void` — crea `BankStatementMatch` propuestos para todas las líneas pending del import (Plan 03-03).

`ConfirmBankStatementMatch::handle(BankStatementMatch $match, ?int $userId = null): void` — confirma un match, marca Payments, descarta hermanos (Plan 03-03).

`BankProfile::cases()` / `getLabel()` — para el `Select` de banco (Plan 03-01). `BankStatementLineStatus`, `BankStatementMatchConfidence`, `BankStatementMatchStatus` — para badges (`->badge()` en `TextColumn`, patrón confirmado en `app/Filament/Resources/PaymentOrders/Tables/PaymentOrdersTable.php:35`).

`CurrentCompany::get(): Company` (app/Services/Accounting/CurrentCompany.php) — para resolver la `Company` activa, igual que `CompanySettings`/`BankReconciliationReport`.

Copy exacto de `03-UI-SPEC.md` (no paráfrasis):
- CTA subida: "Importar extracto"
- CTA confirmar: "Confirmar cruce"
- Acción descartar: "Descartar sugerencia"
- Empty state: heading "Todo conciliado", body "No quedan líneas pendientes por revisar en este extracto."
- Error encabezados (D-03): "El archivo no tiene encabezados de columna reconocibles. Verifica que sea un CSV exportado del banco seleccionado y vuelve a intentarlo."
- Error solape (D-05): "Este archivo se solapa con un extracto ya importado para esta cuenta (rango {starts_on}–{ends_on}). Recorta el archivo para cubrir solo fechas nuevas y vuelve a intentarlo."
- Banner rechazadas heading: "Filas rechazadas"; body: "{n} fila(s) no se pudieron leer y no se importaron. Revisa el motivo de cada una y corrige el archivo original antes de reintentar."
- Colores: accent (`primary`, ya mapeado a Amber) SOLO para "Importar extracto"/"Confirmar cruce"; `danger` para "Descartar sugerencia" y el banner de rechazadas; confidence badges usan `success`/`gray` (nunca `warning`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: UploadBankStatement — formulario CashAccount+BankProfile+FileUpload</name>
  <files>app/Filament/Pages/UploadBankStatement.php, resources/views/filament/pages/upload-bank-statement.blade.php, tests/Feature/BankStatementUploadTest.php</files>
  <read_first>
    - app/Filament/Pages/CompanySettings.php (patrón exacto: Page implements HasForms, use InteractsWithForms, form(Schema), statePath('data'), getFormActions())
    - app/Providers/Filament/AdminPanelProvider.php (confirma discoverPages automático — no requiere registro manual; navigationGroup 'Tesorería' ya existe en la lista de grupos)
  </read_first>
  <action>
Crear `app/Filament/Pages/UploadBankStatement.php` siguiendo el patrón de `CompanySettings.php`:
- `implements HasForms`, `use InteractsWithForms`.
- `$navigationIcon = Heroicon::ArrowUpTray`, `$navigationLabel = 'Importar Extracto Bancario'`, `$navigationGroup = 'Tesorería'`, `$title = 'Importar Extracto Bancario'`.
- `protected string $view = 'filament.pages.upload-bank-statement';`
- `public ?array $data = [];`
- `form(Schema $schema)`: `Select::make('cash_account_id')->label('Cuenta bancaria')->options(fn () => CashAccount::query()->whereBelongsTo(app(CurrentCompany::class)->get())->where('is_active', true)->pluck('name', 'id'))->searchable()->preload()->required()`; `Select::make('bank')->label('Banco')->options(BankProfile::class)->required()->native(false)`; `FileUpload::make('file')->label('Archivo CSV')->disk('local')->directory('bank-statements')->visibility('private')->extensions(['csv', 'txt'])->required()`. `->statePath('data')`.
- `getFormActions()`: `Action::make('import')->label('Importar extracto')->color('primary')->submit('import')`.
- Método `import()`: resolver `$cashAccount = CashAccount::findOrFail($data['cash_account_id'])`, `$bank = BankProfile::from($data['bank'])`, `$absolutePath = Storage::disk('local')->path($data['file'])`; llamar `$import = app(ImportBankStatement::class)->handle(app(CurrentCompany::class)->get(), $cashAccount, $bank, $absolutePath)`; luego `app(ProposeBankStatementMatches::class)->handle($import)`; si todo va bien, `redirect(BankStatementReview::getUrl(['import' => $import->id]))`. Dejar que la `ValidationException` lanzada por `ImportBankStatement` propague sin capturarla — Filament la traduce automáticamente en errores de formulario sobre el campo `file` (mismo mecanismo que cualquier Form Request/servicio existente en el proyecto).
- Agregar una tabla secundaria de "Imports recientes" en `content(Schema $schema)` (usar `Section` + un `TextColumn`/lista simple, o reutilizar `EmbeddedTable` de un segundo `HasTable` NO es necesario aquí — basta un `Filament\Schemas\Components\Text`/`RepeatableEntry` simple listando `BankStatementImport::query()->latest()->limit(10)->get()` con banco, cuenta, rango de fechas, y un link a `BankStatementReview::getUrl(['import' => $row->id])` por cada uno usando `Filament\Schemas\Components\Actions\Action` embebido o un simple `Text::make(...)->url(...)`). Mantenerlo simple: una lista de texto con enlaces, no una tabla `HasTable` completa (evita duplicar infraestructura de tabla en esta página).

Crear `resources/views/filament/pages/upload-bank-statement.blade.php`:
```blade
<x-filament-panels::page>
    {{ $this->form }}
</x-filament-panels::page>
```
  </action>
  <verify>
    <automated>php artisan test --compact --filter=BankStatementUploadTest</automated>
  </verify>
  <acceptance_criteria>
    - Test: subir un CSV válido (fixture Bancolombia con 2 filas) vía `Livewire::test(UploadBankStatement::class)->fillForm([...])->call('import')` termina en `assertHasNoFormErrors()` y crea 1 `BankStatementImport` + 2 `BankStatementLine`.
    - Test: subir un CSV sin encabezados reconocibles produce `assertHasFormErrors(['file'])` con el mensaje exacto de D-03 y `BankStatementImport::count()` no cambia.
    - Test: subir un segundo CSV con rango solapado para la misma CashAccount produce `assertHasFormErrors(['file'])` con el mensaje conteniendo "se solapa" (D-05).
    - `grep -q "ImportBankStatement::class" app/Filament/Pages/UploadBankStatement.php`
    - `grep -q "ProposeBankStatementMatches::class" app/Filament/Pages/UploadBankStatement.php`
    - `grep -q "'Importar extracto'" app/Filament/Pages/UploadBankStatement.php` (copy exacto de 03-UI-SPEC.md)
  </acceptance_criteria>
  <done>Usuario puede subir un CSV desde el panel, ver errores D-03/D-05 con el copy exacto, y llegar a un import creado con matches ya propuestos.</done>
</task>

<task type="auto">
  <name>Task 2: BankStatementReview — tabla de líneas pendientes, banner de rechazadas, empty state</name>
  <files>app/Filament/Pages/BankStatementReview.php, resources/views/filament/pages/bank-statement-review.blade.php, tests/Feature/BankStatementReviewTest.php</files>
  <read_first>
    - app/Filament/Pages/BankReconciliationReport.php (patrón Page implements HasTable, use InteractsWithTable, records(), content(Schema) con EmbeddedTable)
    - app/Filament/Pages/AccountabilityCenter.php (patrón de banner/texto vía Schemas\Components\Text y Section, sin blade custom)
  </read_first>
  <action>
Crear `app/Filament/Pages/BankStatementReview.php` siguiendo el patrón de `BankReconciliationReport.php`:
- `implements HasTable`, `use InteractsWithTable`.
- `$navigationGroup = 'Tesorería'`; `shouldRegisterNavigation(): bool { return false; }` (D-09: página dedicada por import, no navegable globalmente — se llega vía redirect o el link de la lista de imports recientes de Task 1).
- `public ?int $importId = null;` y `public function mount(): void { $this->importId = (int) request()->query('import'); }`.
- `$title`: sobreescribir dinámicamente con `getTitle(): string` retornando `"Revisión de extracto — {$this->import()->bank->getLabel()} · {$this->import()->cashAccount->name}"` (Display heading, D-09 copy).
- Método `import(): BankStatementImport { return BankStatementImport::query()->with('cashAccount')->findOrFail($this->importId); }`.
- `table(Table $table)`: `->records()` con query `BankStatementLine::query()->where('bank_statement_import_id', $this->importId)->where('status', BankStatementLineStatus::Pending)->with('matches.payments')->orderBy('line_date')`. Columnas: `TextColumn::make('line_date')->label('Fecha')->date('Y-m-d')`; `TextColumn::make('description')->label('Descripción')`; `TextColumn::make('amount')->label('Monto')->money('COP')->alignEnd()`; `TextColumn::make('reference')->label('Referencia')->placeholder('—')`; columna calculada `TextColumn::make('candidates')->label('Candidatos')->state(fn (BankStatementLine $record) => $record->matches->where('status', BankStatementMatchStatus::Proposed)->count())->badge()->color(fn (int $state) => $state > 0 ? 'success' : 'gray')`.
- Acciones por fila: `ActionGroup::make(fn (BankStatementLine $record): array => $record->matches->where('status', BankStatementMatchStatus::Proposed)->flatMap(fn (BankStatementMatch $match) => [Action::make("confirm-{$match->id}")->label('Confirmar cruce')->color('primary')->action(fn () => app(ConfirmBankStatementMatch::class)->handle($match, auth()->id())), Action::make("discard-{$match->id}")->label('Descartar sugerencia')->color('danger')->action(fn () => $match->update(['status' => BankStatementMatchStatus::Discarded]))])->all())`.
- `->emptyStateHeading('Todo conciliado')->emptyStateDescription('No quedan líneas pendientes por revisar en este extracto.')`.
- `content(Schema $schema)`: componer `Text`/`Section` (patrón `AccountabilityCenter.php`) con un banner visible solo si hay rechazadas: heading "Filas rechazadas" (color danger), body "{n} fila(s) no se pudieron leer y no se importaron. Revisa el motivo de cada una y corrige el archivo original antes de reintentar." seguido de un `UnorderedList::make($rejectedReasons)`; luego `EmbeddedTable::make()`.

Crear `resources/views/filament/pages/bank-statement-review.blade.php`:
```blade
<x-filament-panels::page>
    {{ $this->content }}
</x-filament-panels::page>
```
Nota: si el patrón de `BankReconciliationReport` usa `{{ $this->table }}` directamente en vez de `{{ $this->content }}`, replicar exactamente ese patrón en su lugar — verificar contra el archivo leído en `read_first` antes de decidir.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=BankStatementReviewTest</automated>
  </verify>
  <acceptance_criteria>
    - Test: un import con 1 línea pending + 1 match propuesto (High) → `Livewire::test(BankStatementReview::class, ['import' => $import->id])->assertSuccessful()->assertSee('Confirmar cruce')`.
    - Test: invocar la acción `confirm-{id}` marca el Payment `is_reconciled=true` (verificar `$payment->fresh()->is_reconciled`) y la línea desaparece de la tabla de pendientes tras refrescar (ya no aparece en `->assertCanSeeTableRecords()`).
    - Test: invocar la acción `discard-{id}` NO modifica ningún Payment, solo el `BankStatementMatch.status` pasa a Discarded.
    - Test: un import sin líneas pending (todas matched/rejected) muestra el texto "Todo conciliado".
    - Test: un import con al menos 1 línea rejected muestra el heading "Filas rechazadas" y el motivo de rechazo.
    - `grep -q "ConfirmBankStatementMatch::class" app/Filament/Pages/BankStatementReview.php`
    - `grep -q "shouldRegisterNavigation" app/Filament/Pages/BankStatementReview.php`
  </acceptance_criteria>
  <done>Usuario ve las líneas pendientes de un import específico con sus candidatos, confirma/descarta individualmente sin confirmación automática, ve las rechazadas, y llega a "Todo conciliado" cuando termina.</done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=BankStatementUploadTest` y `--filter=BankStatementReviewTest` — todos en verde.
- Flujo manual (smoke, opcional): subir un CSV real vía `/admin` en Herd, confirmar redirect a la página de revisión con el import correcto.
- `vendor/bin/pint --dirty --format agent` limpio sobre los 2 archivos de página.
</verification>

<success_criteria>
BANKREC-01 y BANKREC-05 quedan cerrados de punta a punta desde el panel: un usuario real puede subir un extracto, ver los errores explícitos de D-03/D-05 con el copy exacto de 03-UI-SPEC.md, y confirmar/descartar cada sugerencia de cruce sin que nada se confirme automáticamente. Con este plan, BANKREC-01 a BANKREC-06 quedan completos en conjunto con Plans 01-03.
</success_criteria>

<output>
After completion, create `.planning/phases/03-conciliaci-n-bancaria-csv-fase-b/03-04-SUMMARY.md`
</output>
