---
phase: 04-hook-de-facturaci-n-externa-fase-e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_09_18_090000_create_external_invoice_references_table.php
  - app/Models/ExternalInvoiceReference.php
  - app/Models/IncomeRecord.php
  - database/factories/ExternalInvoiceReferenceFactory.php
  - app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php
  - tests/Feature/ExternalInvoiceReferenceTest.php
autonomous: true
requirements: [INVHOOK-01, INVHOOK-02, INVHOOK-03]

must_haves:
  truths:
    - "Usuario puede registrar manualmente una referencia de factura externa (invoice_number, cufe, provider, document_url) para un IncomeRecord ya creado, desde una acción de tabla en Ingresos"
    - "Usuario puede reabrir la misma acción sobre un IncomeRecord que ya tiene referencia y ver/editar los valores ya guardados, incluyendo re-guardar sin cambiar invoice_number"
    - "La referencia vive en su propia tabla (external_invoice_references) con FK única a income_records; el IncomeRecord y su Voucher nunca se modifican por esta acción"
    - "Ningún código nuevo de esta fase realiza llamadas HTTP salientes (sin integración activa, INVHOOK-03)"
  artifacts:
    - path: "database/migrations/2026_09_18_090000_create_external_invoice_references_table.php"
      provides: "Esquema de external_invoice_references con FK única a income_records"
      contains: "external_invoice_references"
    - path: "app/Models/ExternalInvoiceReference.php"
      provides: "Modelo relacionado 1:1, sin Auditable, sin llamadas HTTP"
      contains: "class ExternalInvoiceReference"
    - path: "app/Models/IncomeRecord.php"
      provides: "Relación hasOne(ExternalInvoiceReference::class)"
      contains: "externalInvoiceReference"
    - path: "database/factories/ExternalInvoiceReferenceFactory.php"
      provides: "Factory para tests"
      exports: ["ExternalInvoiceReferenceFactory"]
    - path: "app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php"
      provides: "Acción de tabla 'external_invoice' con label/fillForm condicional y updateOrCreate"
      contains: "external_invoice"
    - path: "tests/Feature/ExternalInvoiceReferenceTest.php"
      provides: "Cobertura Pest de modelo + acción"
      min_lines: 40
  key_links:
    - from: "app/Models/IncomeRecord.php"
      to: "app/Models/ExternalInvoiceReference.php"
      via: "hasOne relation"
      pattern: "hasOne\\(ExternalInvoiceReference::class\\)"
    - from: "app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php"
      to: "app/Models/ExternalInvoiceReference.php"
      via: "updateOrCreate sobre la relación hasOne dentro del closure de la Action"
      pattern: "externalInvoiceReference\\(\\)->updateOrCreate"
---

<objective>
Permitir que el usuario deje registro auditable de una referencia de factura electrónica emitida por un proveedor tercero (número, CUFE, proveedor, URL del documento) asociada a un `IncomeRecord` ya creado, sin modificar el `IncomeRecord`/`Voucher` inmutable y sin ninguna integración activa (API/webhook).

Purpose: Cerrar INVHOOK-01/02/03 — captura manual acotada, sin comprometer el Core Value de inmutabilidad contable ni introducir integraciones prematuras.
Output: Tabla `external_invoice_references` + modelo `ExternalInvoiceReference` + relación `IncomeRecord::externalInvoiceReference()` + acción de tabla "Registrar/Ver factura externa" en `IncomeRecordsTable`, con tests Pest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hook-de-facturaci-n-externa-fase-e/04-CONTEXT.md
@.planning/phases/04-hook-de-facturaci-n-externa-fase-e/04-RESEARCH.md

<interfaces>
<!-- Extraído directamente del código del proyecto. El executor debe usar esto sin explorar más. -->

From `app/Models/IncomeRecord.php` (estado actual, antes de este plan):
```php
class IncomeRecord extends Model
{
    /** @use HasFactory<IncomeRecordFactory> */
    use HasFactory;

    protected $fillable = [
        'voucher_id', 'budget_revenue_id', 'revenue_account_id',
        'receivable_account_id', 'support_number', 'accrual_date', 'amount',
    ];

    public function voucher(): BelongsTo { return $this->belongsTo(Voucher::class); }
    // ... revenueAccount(), receivableAccount(), budgetRevenue()
}
```
No tiene página `edit` registrada (`IncomeRecordResource::getPages()` solo define `'index'` y `'create'`) — la tabla se renderiza en `ListIncomeRecords`, confirmado con `php artisan route:list --name=income-records` (solo 2 rutas: index, create). La acción nueva se prueba con `Livewire::test(ListIncomeRecords::class)`.

From `app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php` (estado actual completo — archivo a editar):
```php
class IncomeRecordsTable
{
    public static function configure(Table $table): Table
    {
        return $table
            ->defaultSort('accrual_date', 'desc')
            ->columns([
                TextColumn::make('accrual_date')->label('Fecha')->date()->sortable(),
                TextColumn::make('voucher.number')->label('Comprobante')->searchable(),
                TextColumn::make('voucher.thirdParty.name')->label('Tercero')->searchable(),
                TextColumn::make('support_number')->label('Soporte')->searchable(),
                TextColumn::make('revenueAccount.full_name')->label('Ingreso')->toggleable(),
                TextColumn::make('amount')->label('Valor')->money('COP')->sortable(),
            ]);
    }
}
```
No tiene `->recordActions([...])` todavía — hay que agregarlo.

From `app/Filament/Resources/Quotations/Tables/QuotationsTable.php` (precedente exacto de Action con form modal + updateOrCreate-like persistencia, líneas 64-85):
```php
Action::make('reject')
    ->label('Rechazar')
    ->icon(Heroicon::XCircle)
    ->color('danger')
    ->modalHeading(fn (Quotation $record): string => "Rechazar {$record->number}")
    ->form([
        Textarea::make('rejection_reason')->label('Motivo de rechazo')->required()->maxLength(255),
    ])
    ->action(function (Quotation $record, array $data): void {
        $record->forceFill([...])->save();
        Notification::make()->success()->title('Cotización rechazada')->send();
    }),
```

From `vendor/filament/forms/src/Components/Concerns/CanBeValidated.php:563` (firma verificada de `unique()`):
```php
public function unique(
    string|Closure|null $table = null,
    string|Closure|null $column = null,
    Model|Closure|null $ignorable = null,
    ?bool $ignoreRecord = false,
    ?Closure $modifyRuleUsing = null,
): static
```
Cuando `ignoreRecord: true`, el default de `$ignorable` es `$component->getRecord()` — en el contexto de esta Action ese default resuelve al `IncomeRecord` de la fila, NO al `ExternalInvoiceReference`. Por eso `$ignorable` debe pasarse explícito como closure que retorna `$record->externalInvoiceReference`.

From `database/migrations/2026_09_16_210230_create_quotations_table.php:25` (convención FK única 1:1 correcta a seguir, NO copiar el hueco de `payment_orders.budget_obligation_id` que no lleva `unique()`):
```php
$table->foreignId('voucher_id')->nullable()->unique()->constrained('vouchers')->nullOnDelete();
```
Para esta fase, `income_record_id` NO es nullable (la referencia solo se crea sobre un `IncomeRecord` ya existente) — usar `cascadeOnDelete()`, igual que `income_records.voucher_id` (`$table->foreignId('voucher_id')->constrained()->cascadeOnDelete();`).

From `tests/Feature/QuotationLifecycleTest.php` (patrón de test de Action con form, líneas 57-67):
```php
Livewire::test(ListQuotations::class)
    ->callAction(TestAction::make('reject')->table($quotation), ['rejection_reason' => ''])
    ->assertHasFormErrors(['rejection_reason' => 'required']);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migración, modelo ExternalInvoiceReference, relación en IncomeRecord y factory</name>
  <files>database/migrations/2026_09_18_090000_create_external_invoice_references_table.php, app/Models/ExternalInvoiceReference.php, app/Models/IncomeRecord.php, database/factories/ExternalInvoiceReferenceFactory.php, tests/Feature/ExternalInvoiceReferenceTest.php</files>
  <read_first>
    app/Models/IncomeRecord.php
    app/Models/BudgetObligation.php
    app/Models/Voucher.php
    database/migrations/2026_09_16_210230_create_quotations_table.php
    database/factories/IncomeRecordFactory.php
  </read_first>
  <behavior>
    En `tests/Feature/ExternalInvoiceReferenceTest.php` (nuevo archivo, `it()` — el proyecto usa `it()` en `QuotationLifecycleTest.php`):
    - Test 1: "un IncomeRecord sin referencia devuelve null en externalInvoiceReference" — crear `IncomeRecord::factory()->create()`, `expect($incomeRecord->externalInvoiceReference)->toBeNull()`.
    - Test 2: "un IncomeRecord puede tener una ExternalInvoiceReference asociada vía hasOne" — `ExternalInvoiceReference::factory()->for($incomeRecord)->create(['invoice_number' => 'FE-100'])`, luego `expect($incomeRecord->fresh()->externalInvoiceReference->invoice_number)->toBe('FE-100')`.
    - Test 3: "income_record_id es único a nivel de base de datos" — crear una `ExternalInvoiceReference` para un `IncomeRecord`, luego intentar crear una segunda para el MISMO `income_record_id` (usando `ExternalInvoiceReference::factory()->create(['income_record_id' => $incomeRecord->id, 'invoice_number' => 'FE-200'])`) y esperar que lance `Illuminate\Database\QueryException` (`expect(fn () => ...)->toThrow(\Illuminate\Database\QueryException::class)`).
  </behavior>
  <action>
    1. Crear la migración `database/migrations/2026_09_18_090000_create_external_invoice_references_table.php` con exactamente:
    ```php
    Schema::create('external_invoice_references', function (Blueprint $table) {
        $table->id();
        $table->foreignId('income_record_id')->unique()->constrained()->cascadeOnDelete();
        $table->string('invoice_number')->unique();
        $table->string('cufe')->nullable();
        $table->string('provider')->nullable();
        $table->string('document_url')->nullable();
        $table->timestamps();
    });
    ```
    `down()` con `Schema::dropIfExists('external_invoice_references')`. Ejecutar `php artisan migrate` tras crearla.

    2. Crear `app/Models/ExternalInvoiceReference.php`:
    ```php
    <?php

    namespace App\Models;

    use Database\Factories\ExternalInvoiceReferenceFactory;
    use Illuminate\Database\Eloquent\Factories\HasFactory;
    use Illuminate\Database\Eloquent\Model;
    use Illuminate\Database\Eloquent\Relations\BelongsTo;

    /**
     * Referencia manual de una factura electrónica emitida por un proveedor tercero.
     * Sin integración activa (INVHOOK-03) — no realiza llamadas HTTP; ver INVHOOK-04 (v2) para integración futura.
     */
    class ExternalInvoiceReference extends Model
    {
        /** @use HasFactory<ExternalInvoiceReferenceFactory> */
        use HasFactory;

        protected $fillable = [
            'income_record_id',
            'invoice_number',
            'cufe',
            'provider',
            'document_url',
        ];

        public function incomeRecord(): BelongsTo
        {
            return $this->belongsTo(IncomeRecord::class);
        }
    }
    ```
    NO usar el trait `Auditable` (consistente con `IncomeRecord`/`ExpenseRecord`; D-03 confirma que no requiere historial de corrección).

    3. Editar `app/Models/IncomeRecord.php`: agregar el import `use Illuminate\Database\Eloquent\Relations\HasOne;` y el método:
    ```php
    public function externalInvoiceReference(): HasOne
    {
        return $this->hasOne(ExternalInvoiceReference::class);
    }
    ```
    No modificar `$fillable`, `casts()` ni las relaciones existentes.

    4. Crear `database/factories/ExternalInvoiceReferenceFactory.php`:
    ```php
    <?php

    namespace Database\Factories;

    use App\Models\ExternalInvoiceReference;
    use App\Models\IncomeRecord;
    use Illuminate\Database\Eloquent\Factories\Factory;

    /**
     * @extends Factory<ExternalInvoiceReference>
     */
    class ExternalInvoiceReferenceFactory extends Factory
    {
        public function definition(): array
        {
            return [
                'income_record_id' => IncomeRecord::factory(),
                'invoice_number' => fake()->unique()->bothify('FE-####'),
                'cufe' => null,
                'provider' => fake()->company(),
                'document_url' => null,
            ];
        }
    }
    ```

    5. Crear `tests/Feature/ExternalInvoiceReferenceTest.php` con los 3 tests descritos en `<behavior>`. Ejecutar RED (deben fallar si el modelo/migración no existieran) solo mentalmente — implementar migración+modelo+factory ANTES de correr los tests es aceptable aquí porque es trabajo estructural, no lógica de negocio a descubrir por TDD puro; el orden migración→modelo→factory→test sigue siendo válido siempre que el comando de verificación (GREEN) se ejecute al final de este task.
  </action>
  <verify>
    <automated>php artisan test --compact tests/Feature/ExternalInvoiceReferenceTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `database/migrations/2026_09_18_090000_create_external_invoice_references_table.php` existe y contiene `external_invoice_references`, `income_record_id`, `->unique()`, `cascadeOnDelete`
    - `php artisan migrate --pretend` (o `php artisan migrate`) no reporta error de migración pendiente/fallida para este archivo
    - `app/Models/ExternalInvoiceReference.php` contiene `class ExternalInvoiceReference` y `belongsTo(IncomeRecord::class)`; NO contiene `Http::` ni `use Illuminate\Support\Facades\Http;`
    - `app/Models/IncomeRecord.php` contiene `function externalInvoiceReference(): HasOne` y `hasOne(ExternalInvoiceReference::class)`
    - `database/factories/ExternalInvoiceReferenceFactory.php` existe y extiende `Factory`
    - `php artisan test --compact tests/Feature/ExternalInvoiceReferenceTest.php` sale con código 0 y reporta 3 tests pasando
  </acceptance_criteria>
  <done>Migración, modelo, relación y factory existen; los 3 tests de modelo pasan, incluida la garantía de unicidad de `income_record_id` a nivel de base de datos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Acción de tabla "Registrar/Ver factura externa" en IncomeRecordsTable</name>
  <files>app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php, tests/Feature/ExternalInvoiceReferenceTest.php</files>
  <read_first>
    app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php
    app/Filament/Resources/Quotations/Tables/QuotationsTable.php
    app/Filament/Resources/IncomeRecords/Pages/ListIncomeRecords.php
    tests/Feature/QuotationLifecycleTest.php
    app/Models/ExternalInvoiceReference.php
  </read_first>
  <behavior>
    Agregar a `tests/Feature/ExternalInvoiceReferenceTest.php` (mismo archivo del Task 1, `it()`):
    - Test 4: "registra una referencia de factura externa sobre un IncomeRecord existente" — `Livewire::test(ListIncomeRecords::class)->callAction(TestAction::make('external_invoice')->table($incomeRecord), ['invoice_number' => 'FE-001', 'cufe' => null, 'provider' => 'Proveedor X', 'document_url' => null])`, luego `expect($incomeRecord->fresh()->externalInvoiceReference->invoice_number)->toBe('FE-001')`.
    - Test 5: "exige invoice_number para registrar la factura externa" — `callAction(..., ['invoice_number' => ''])->assertHasFormErrors(['invoice_number' => 'required'])`.
    - Test 6: "permite editar una referencia existente reenviando el mismo invoice_number sin error de unicidad" — crear `$incomeRecord` con `ExternalInvoiceReference::factory()->for($incomeRecord)->create(['invoice_number' => 'FE-777'])`, luego `callAction(TestAction::make('external_invoice')->table($incomeRecord), ['invoice_number' => 'FE-777', 'provider' => 'Proveedor Actualizado'])` sin errores de formulario, y `expect($incomeRecord->fresh()->externalInvoiceReference->provider)->toBe('Proveedor Actualizado')`.
    - Test 7: "el modal se pre-rellena con los datos existentes al editar" — con el mismo fixture del Test 6, `Livewire::test(ListIncomeRecords::class)->mountAction(TestAction::make('external_invoice')->table($incomeRecord))->assertSchemaStateSet(['invoice_number' => 'FE-777'])` (usar `assertSchemaStateSet` sobre el `mountedActionSchema`; si el helper exacto difiere en esta versión de Filament, usar el equivalente ya usado en otros tests del proyecto para verificar estado de formulario montado — revisar `tests/Feature/*.php` existentes por precedente de `mountAction`/`assertSchemaStateSet` antes de escribir este test).
  </behavior>
  <action>
    Editar `app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php`: agregar imports `use App\Models\ExternalInvoiceReference; use App\Models\IncomeRecord; use Filament\Actions\Action; use Filament\Forms\Components\TextInput; use Filament\Notifications\Notification; use Filament\Support\Icons\Heroicon;` y agregar `->recordActions([...])` al final del builder existente (después de `->columns([...])`, sin tocar las columnas actuales):
    ```php
    ->recordActions([
        Action::make('external_invoice')
            ->label(fn (IncomeRecord $record): string => $record->externalInvoiceReference
                ? 'Ver/editar factura externa'
                : 'Registrar factura externa')
            ->icon(Heroicon::DocumentText)
            ->color('gray')
            ->fillForm(fn (IncomeRecord $record): array => $record->externalInvoiceReference?->toArray() ?? [])
            ->form([
                TextInput::make('invoice_number')
                    ->label('Número de factura')
                    ->required()
                    ->unique(
                        table: 'external_invoice_references',
                        column: 'invoice_number',
                        ignorable: fn (IncomeRecord $record): ?ExternalInvoiceReference => $record->externalInvoiceReference,
                        ignoreRecord: true,
                    )
                    ->maxLength(255),
                TextInput::make('cufe')->label('CUFE')->maxLength(255),
                TextInput::make('provider')->label('Proveedor')->maxLength(255),
                TextInput::make('document_url')->label('URL del documento')->url()->maxLength(255),
            ])
            ->action(function (IncomeRecord $record, array $data): void {
                $record->externalInvoiceReference()->updateOrCreate([], $data);

                Notification::make()->success()->title('Factura externa registrada')->send();
            }),
    ]);
    ```
    Verificar que `Heroicon::DocumentText` existe (ya confirmado: `Heroicon::DocumentText->value === 'document-text'`). NO agregar ninguna llamada `Http::`, cliente HTTP, ni referencia a un proveedor de facturación electrónica (INVHOOK-03). NO tocar `IncomeRecordForm.php`, `CreateIncomeRecord.php` ni `EditIncomeRecord.php` (D-01).
  </action>
  <verify>
    <automated>php artisan test --compact tests/Feature/ExternalInvoiceReferenceTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php` contiene `Action::make('external_invoice')`, `->fillForm(`, `updateOrCreate`, `ignorable:`, `ignoreRecord: true`
    - `app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php` NO contiene `Http::` ni `use Illuminate\Support\Facades\Http;`
    - `grep -n "IncomeRecordForm\|CreateIncomeRecord\|EditIncomeRecord" app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php` no produce coincidencias (archivo no toca esas clases)
    - `php artisan test --compact tests/Feature/ExternalInvoiceReferenceTest.php` sale con código 0, 7 tests pasando en total (3 del Task 1 + 4 de este task)
    - `vendor/bin/pint --dirty --format agent` no reporta cambios pendientes tras ejecutarlo
  </acceptance_criteria>
  <done>La acción "Registrar/Ver factura externa" existe en `IncomeRecordsTable`, persiste vía `updateOrCreate` sobre la relación `hasOne`, valida `invoice_number` único excluyendo el propio registro en edición, y los 7 tests Pest de la fase pasan.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact tests/Feature/ExternalInvoiceReferenceTest.php` — 7 tests, exit 0.
2. `php artisan test --compact` (suite completa) — sin regresiones en `IncomeRecord`/`Quotation`/reportes existentes.
3. `vendor/bin/pint --dirty --format agent` — limpio, sin cambios de formato pendientes.
4. `grep -rn "Http::" app/Models/ExternalInvoiceReference.php app/Filament/Resources/IncomeRecords/Tables/IncomeRecordsTable.php` — sin resultados (INVHOOK-03).
5. Inspección manual: `income_records` (migración existente) no tiene columnas nuevas — la referencia vive únicamente en `external_invoice_references` (INVHOOK-02).
</verification>

<success_criteria>
- Usuario puede registrar y editar una referencia de factura externa (número, CUFE, proveedor, URL) para un `IncomeRecord` ya creado, desde una acción de tabla, sin tocar el formulario de edición del `IncomeRecord`.
- La referencia vive en `external_invoice_references`, tabla propia con FK única `income_record_id`; `income_records`/`vouchers` permanecen sin cambios de esquema.
- Ningún código nuevo de esta fase realiza llamadas HTTP salientes.
- INVHOOK-01, INVHOOK-02, INVHOOK-03 quedan con soporte de test automatizado.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hook-de-facturaci-n-externa-fase-e/04-01-SUMMARY.md`
</output>
