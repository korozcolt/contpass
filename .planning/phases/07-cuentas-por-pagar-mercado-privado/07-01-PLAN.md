---
phase: 07-cuentas-por-pagar-mercado-privado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Accounting/AccountsPayable.php
  - tests/Feature/AccountsPayableReportTest.php
autonomous: true
requirements: [AP-01]

must_haves:
  truths:
    - "AccountsPayable::openItems() incluye ExpenseRecords de mercado privado (budget_obligation_id nulo), no solo BudgetObligation"
    - "Una fila de mercado privado muestra el número de comprobante del voucher (expenseRecord.voucher.number) en la clave 'number', igual al patrón 'voucher_number' de AccountsReceivable (D-02)"
    - "Una fila de mercado privado reporta el valor neto de retención (expenseRecord.amount - expenseRecord.withholding_amount), no el bruto (D-03)"
    - "Un ExpenseRecord de mercado privado totalmente pagado (via Payment.source_voucher_id contra su voucher) queda excluido del resultado (pending <= 0.01)"
    - "Las filas de BudgetObligation público (payment_order_id, BudgetObligation.amount bruto) siguen funcionando exactamente igual que antes de este plan — regresión cero en el camino público"
    - "El reporte en pantalla, su CSV y su export Excel muestran las mismas filas combinadas sin lógica de consulta duplicada entre los tres (ya garantizado porque los tres consumen accountsPayableRows() -> AccountsPayable::openItems() sin cambios en el controlador ni en la página Filament)"
  artifacts:
    - path: "app/Services/Accounting/AccountsPayable.php"
      provides: "openItems() combina BudgetObligation público + ExpenseRecord privado en una sola Collection, misma firma pública, mismo shape de array de salida"
      contains: "budget_obligation_id"
    - path: "tests/Feature/AccountsPayableReportTest.php"
      provides: "Cobertura Pest de las filas de mercado privado (inclusión, neto de retención, exclusión al pagar) + regresión del camino público existente"
      contains: "private-market"
  key_links:
    - from: "app/Services/Accounting/AccountsPayable.php"
      to: "app/Models/Payment.php"
      via: "Payment::query()->whereIn('source_voucher_id', ...) agrupado por source_voucher_id, mismo patrón que AccountsReceivable::openItems()"
      pattern: "source_voucher_id"
    - from: "app/Services/Accounting/AccountsPayable.php"
      to: "app/Models/ExpenseRecord.php"
      via: "ExpenseRecord::query()->whereNull('budget_obligation_id')->whereHas('voucher', ...)"
      pattern: "whereNull\\('budget_obligation_id'\\)"
    - from: "app/Http/Controllers/AccountingReportController.php"
      to: "app/Services/Accounting/AccountsPayable.php"
      via: "accountsPayableRows() (línea ~325) llama a AccountsPayable::openItems() sin cambios — consumido igual por CSV (accountsPayable()) y Excel (accountsPayableXlsx())"
      pattern: "AccountsPayable::class\\)->openItems"
---

<objective>
Extender `AccountsPayable::openItems()` para incluir también los `ExpenseRecord`s de mercado privado (`budget_obligation_id` nulo), usando exactamente el patrón `source_voucher_id` ya usado por el servicio hermano `AccountsReceivable::openItems()`, sin tocar la lógica existente de `BudgetObligation`/`payment_order_id`.

Purpose: cerrar AP-01 — el gap `accounts-payable-private-market-scope` del audit v1.0. Hoy el reporte "cuentas por pagar" (pantalla Filament, CSV, Excel) solo refleja obligaciones presupuestales públicas; un `ExpenseRecord` creado por el flujo de gasto de mercado privado (`CreateExpenseRecord` → `PostExpenseVoucher::handle()` sin `$obligation`) nunca aparece, aunque tenga retención ICA (Fase 2) o esté conciliado (Fase 3).
Output: `AccountsPayable::openItems()` devuelve una `Collection` combinada (misma firma pública, mismo shape de array — `third_party`, `number`, `support_number`, `accrual_date`, `amount`, `paid`, `pending`, `days_overdue`, `bucket`, más `budget_obligation_id` ahora explícito y nulo para filas privadas); tests Pest nuevos cubriendo inclusión, neto de retención, exclusión al pago total y regresión del camino público; suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cuentas-por-pagar-mercado-privado/07-CONTEXT.md

<interfaces>
<!-- Extraído directamente del código del proyecto durante planning (2026-09-18). El executor debe usar esto sin re-explorar el codebase. -->

**Estado actual completo de `app/Services/Accounting/AccountsPayable.php` (a modificar):**
```php
<?php

namespace App\Services\Accounting;

use App\Enums\BudgetObligationStatus;
use App\Models\BudgetObligation;
use App\Models\Company;
use App\Models\Payment;
use Illuminate\Support\Collection;

class AccountsPayable
{
    public function openItems(Company $company): Collection
    {
        $obligations = BudgetObligation::query()
            ->with(['budgetRegistration.thirdParty', 'paymentOrder'])
            ->whereBelongsTo($company)
            ->where('status', '!=', BudgetObligationStatus::Cancelled->value)
            ->get();

        $paymentOrderIds = $obligations->pluck('paymentOrder.id')->filter();

        $paidByPaymentOrder = Payment::query()
            ->whereIn('payment_order_id', $paymentOrderIds)
            ->selectRaw('payment_order_id, sum(amount) as paid_total')
            ->groupBy('payment_order_id')
            ->pluck('paid_total', 'payment_order_id');

        return $obligations
            ->map(function (BudgetObligation $obligation) use ($paidByPaymentOrder): array {
                $paid = (float) ($paidByPaymentOrder[$obligation->paymentOrder?->id] ?? 0);
                $pending = round((float) $obligation->amount - $paid, 2);
                $daysOverdue = (int) $obligation->accrual_date->diffInDays(now(), absolute: true);

                return [
                    'budget_obligation_id' => $obligation->id,
                    'third_party' => $obligation->budgetRegistration->thirdParty?->name ?? 'Sin tercero',
                    'number' => $obligation->number,
                    'support_number' => $obligation->support_number,
                    'accrual_date' => $obligation->accrual_date,
                    'amount' => (float) $obligation->amount,
                    'paid' => $paid,
                    'pending' => $pending,
                    'days_overdue' => $daysOverdue,
                    'bucket' => $this->bucket($daysOverdue),
                ];
            })
            ->filter(fn (array $row): bool => $row['pending'] > 0.01)
            ->values();
    }

    private function bucket(int $daysOverdue): string
    {
        return match (true) {
            $daysOverdue <= 30 => 'Corriente',
            $daysOverdue <= 60 => '31-60 días',
            $daysOverdue <= 90 => '61-90 días',
            default => '+90 días',
        };
    }
}
```

**Servicio hermano `app/Services/Accounting/AccountsReceivable.php` (plantilla del patrón `source_voucher_id`, NO modificar este archivo):**
```php
public function openItems(Company $company): Collection
{
    $records = IncomeRecord::query()
        ->with(['voucher.thirdParty'])
        ->whereHas('voucher', fn (Builder $query): Builder => $query->whereBelongsTo($company))
        ->get();

    $paidByVoucher = Payment::query()
        ->whereIn('source_voucher_id', $records->pluck('voucher_id'))
        ->selectRaw('source_voucher_id, sum(amount) as paid_total')
        ->groupBy('source_voucher_id')
        ->pluck('paid_total', 'source_voucher_id');

    return $records
        ->map(function (IncomeRecord $income) use ($paidByVoucher): array {
            $paid = (float) ($paidByVoucher[$income->voucher_id] ?? 0);
            $pending = round((float) $income->amount - $paid, 2);
            $daysOverdue = (int) $income->accrual_date->diffInDays(now(), absolute: true);

            return [
                'income_record_id' => $income->id,
                'third_party' => $income->voucher->thirdParty?->name ?? 'Sin tercero',
                'voucher_number' => $income->voucher->number,
                'support_number' => $income->support_number,
                'accrual_date' => $income->accrual_date,
                'amount' => (float) $income->amount,
                'paid' => $paid,
                'pending' => $pending,
                'days_overdue' => $daysOverdue,
                'bucket' => $this->bucket($daysOverdue),
            ];
        })
        ->filter(fn (array $row): bool => $row['pending'] > 0.01)
        ->values();
}
```

**`app/Models/ExpenseRecord.php` (campos y relaciones relevantes, ya existentes, NO modificar este archivo):**
```php
protected $fillable = ['voucher_id', 'budget_obligation_id', 'expense_account_id', 'payable_account_id', 'municipality_id', 'support_type', 'support_number', 'accrual_date', 'amount', 'withholding_amount', 'has_valid_support', 'is_deductible'];

protected function casts(): array
{
    return ['accrual_date' => 'date', 'amount' => 'decimal:2', 'withholding_amount' => 'decimal:2', 'has_valid_support' => 'boolean', 'is_deductible' => 'boolean'];
}

public function voucher(): BelongsTo { return $this->belongsTo(Voucher::class); }
public function budgetObligation(): BelongsTo { return $this->belongsTo(BudgetObligation::class); }
```
Todo `ExpenseRecord` tiene `voucher_id` NOT NULL — nunca hay un `ExpenseRecord` sin voucher que manejar (confirmado en 07-CONTEXT.md).

**`app/Http/Controllers/AccountingReportController.php` (consumidor, NO modificar — la fila cambia de shape, no este método):**
```php
private function accountsPayableRows(): array
{
    return app(AccountsPayable::class)->openItems($this->currentCompany->get())
        ->map(fn (array $row): array => [
            $row['third_party'],
            $row['number'],
            $row['support_number'],
            $row['accrual_date'],
            $row['amount'],
            $row['paid'],
            $row['pending'],
            $row['days_overdue'],
            $row['bucket'],
        ])
        ->all();
}
```
Este método (y `accountsPayable()` CSV / `accountsPayableXlsx()` Excel que lo llaman) NO se tocan en este plan — consumen las mismas 9 claves (`budget_obligation_id` no se lee aquí, puede quedar `null` en filas privadas sin romper nada).

**`app/Services/Accounting/PostExpenseVoucher.php` (líneas 60-75, confirma el origen de D-03 — NO modificar):**
```php
$withholdingAmount = round($withholdings->sum('amount'), 2);
$payableAmount = round($amount - $withholdingAmount, 2);
// ...
ExpenseRecord::query()->create([
    'voucher_id' => $voucher->id,
    'budget_obligation_id' => $obligation?->id,
    // ...
    'amount' => $amount,                       // bruto
    'withholding_amount' => $withholdingAmount, // lo retenido
]);
```
`payable_account_id` recibe `$payableAmount` (neto) — por eso D-03 exige que la fila de "cuentas por pagar" muestre `amount - withholding_amount`, no `amount`.

**`app/Services/Accounting/RegisterPayment.php` (para construir el fixture de test de pago total, NO modificar):**
```php
public function handle(Company $company, CashAccount $cashAccount, array $data, ?Voucher $sourceVoucher = null): Voucher
```
Crea `Payment::query()->create(['voucher_id' => ..., 'source_voucher_id' => $sourceVoucher?->id, ...])`. Para pagar un `ExpenseRecord` de mercado privado en un test, pasar el `Voucher` devuelto por `PostExpenseVoucher::handle()` como `$sourceVoucher` (cuarto argumento).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Combinar ExpenseRecords de mercado privado en AccountsPayable::openItems() con cobertura Pest</name>
  <files>app/Services/Accounting/AccountsPayable.php, tests/Feature/AccountsPayableReportTest.php</files>
  <read_first>
    app/Services/Accounting/AccountsPayable.php
    app/Services/Accounting/AccountsReceivable.php
    app/Models/ExpenseRecord.php
    app/Models/Payment.php
    app/Models/Voucher.php
    app/Services/Accounting/PostExpenseVoucher.php
    app/Services/Accounting/RegisterPayment.php
    tests/Feature/AccountsPayableReportTest.php
    tests/Feature/AccountsReceivableReportTest.php
    .planning/phases/07-cuentas-por-pagar-mercado-privado/07-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1 ("includes a private-market expense record at its net-of-withholding pending amount"): un `ExpenseRecord` creado vía `PostExpenseVoucher::handle()` SIN `$obligation` (por lo tanto `budget_obligation_id` nulo), con una `WithholdingRule` activa que produce retención > 0, aparece en `AccountsPayable::openItems()` con `budget_obligation_id === null`, `number === $voucher->number` (D-02), y `amount`/`pending` iguales a `expenseRecord.amount - expenseRecord.withholding_amount` (D-03), no al bruto.
    - Test 2 ("excludes a fully paid private-market expense record"): registrar un `Payment` con `source_voucher_id` igual al voucher del `ExpenseRecord` (vía `RegisterPayment::handle(..., $sourceVoucher: $voucher)`) por el monto neto exacto excluye esa fila del resultado (`pending <= 0.01`), igual que el filtro ya existente para `BudgetObligation`.
    - Test 3 ("combines budget obligation and private-market expense rows for the same company"): para la MISMA `Company`, una fila `BudgetObligation` (camino público, fixture `payableFixture()` existente) y una fila `ExpenseRecord` privada (`budget_obligation_id` nulo) aparecen ambas en el mismo `Collection` devuelto por una sola llamada a `openItems()`, sin que ninguna interfiera con la otra.
    - Regresión: los 5 tests ya existentes en `tests/Feature/AccountsPayableReportTest.php` (unpaid fully pending, fully paid excluded, partial payment balance, bucket aging, cancelled excluded) siguen pasando SIN modificarlos — confirman que el camino `BudgetObligation`/`payment_order_id` no cambió (D-01: fuera de alcance, ver GitHub Issue #3).
  </behavior>
  <action>
    **1. Reescribir `app/Services/Accounting/AccountsPayable.php` completo:**

    Agregar imports `App\Models\ExpenseRecord` e `Illuminate\Database\Eloquent\Builder`. Extraer la lógica actual de `openItems()` a un método privado `budgetObligationItems(Company $company): Collection` (el cuerpo es EXACTAMENTE el actual, sin el `->filter(...)->values()` final — ese filtro se aplica una sola vez al final sobre el resultado combinado, no duplicado por sub-colección). Agregar un método privado nuevo `privateMarketItems(Company $company): Collection` que replica el patrón exacto de `AccountsReceivable::openItems()` mostrado en `<interfaces>`, pero sobre `ExpenseRecord` filtrado por `whereNull('budget_obligation_id')`, y aplicando D-03 (neto de retención) al campo `amount`:

    ```php
    <?php

    namespace App\Services\Accounting;

    use App\Enums\BudgetObligationStatus;
    use App\Models\BudgetObligation;
    use App\Models\Company;
    use App\Models\ExpenseRecord;
    use App\Models\Payment;
    use Illuminate\Database\Eloquent\Builder;
    use Illuminate\Support\Collection;

    class AccountsPayable
    {
        /**
         * Open (unpaid or partially paid) accounts payable for a company — combines public budget
         * obligations (payment_order_id) with private-market expense records (source_voucher_id, AP-01),
         * aged by their accrual date.
         *
         * @return Collection<int, array<string, mixed>>
         */
        public function openItems(Company $company): Collection
        {
            return $this->budgetObligationItems($company)
                ->concat($this->privateMarketItems($company))
                ->filter(fn (array $row): bool => $row['pending'] > 0.01)
                ->values();
        }

        private function budgetObligationItems(Company $company): Collection
        {
            $obligations = BudgetObligation::query()
                ->with(['budgetRegistration.thirdParty', 'paymentOrder'])
                ->whereBelongsTo($company)
                ->where('status', '!=', BudgetObligationStatus::Cancelled->value)
                ->get();

            $paymentOrderIds = $obligations->pluck('paymentOrder.id')->filter();

            $paidByPaymentOrder = Payment::query()
                ->whereIn('payment_order_id', $paymentOrderIds)
                ->selectRaw('payment_order_id, sum(amount) as paid_total')
                ->groupBy('payment_order_id')
                ->pluck('paid_total', 'payment_order_id');

            return $obligations->map(function (BudgetObligation $obligation) use ($paidByPaymentOrder): array {
                $paid = (float) ($paidByPaymentOrder[$obligation->paymentOrder?->id] ?? 0);
                $pending = round((float) $obligation->amount - $paid, 2);
                $daysOverdue = (int) $obligation->accrual_date->diffInDays(now(), absolute: true);

                return [
                    'budget_obligation_id' => $obligation->id,
                    'third_party' => $obligation->budgetRegistration->thirdParty?->name ?? 'Sin tercero',
                    'number' => $obligation->number,
                    'support_number' => $obligation->support_number,
                    'accrual_date' => $obligation->accrual_date,
                    'amount' => (float) $obligation->amount,
                    'paid' => $paid,
                    'pending' => $pending,
                    'days_overdue' => $daysOverdue,
                    'bucket' => $this->bucket($daysOverdue),
                ];
            });
        }

        private function privateMarketItems(Company $company): Collection
        {
            $records = ExpenseRecord::query()
                ->with(['voucher.thirdParty'])
                ->whereNull('budget_obligation_id')
                ->whereHas('voucher', fn (Builder $query): Builder => $query->whereBelongsTo($company))
                ->get();

            $paidByVoucher = Payment::query()
                ->whereIn('source_voucher_id', $records->pluck('voucher_id'))
                ->selectRaw('source_voucher_id, sum(amount) as paid_total')
                ->groupBy('source_voucher_id')
                ->pluck('paid_total', 'source_voucher_id');

            return $records->map(function (ExpenseRecord $expense) use ($paidByVoucher): array {
                $paid = (float) ($paidByVoucher[$expense->voucher_id] ?? 0);
                $netAmount = round((float) $expense->amount - (float) $expense->withholding_amount, 2);
                $pending = round($netAmount - $paid, 2);
                $daysOverdue = (int) $expense->accrual_date->diffInDays(now(), absolute: true);

                return [
                    'budget_obligation_id' => null,
                    'third_party' => $expense->voucher->thirdParty?->name ?? 'Sin tercero',
                    'number' => $expense->voucher->number,
                    'support_number' => $expense->support_number,
                    'accrual_date' => $expense->accrual_date,
                    'amount' => $netAmount,
                    'paid' => $paid,
                    'pending' => $pending,
                    'days_overdue' => $daysOverdue,
                    'bucket' => $this->bucket($daysOverdue),
                ];
            });
        }

        private function bucket(int $daysOverdue): string
        {
            return match (true) {
                $daysOverdue <= 30 => 'Corriente',
                $daysOverdue <= 60 => '31-60 días',
                $daysOverdue <= 90 => '61-90 días',
                default => '+90 días',
            };
        }
    }
    ```

    Nótese: `bucket()` NO se duplica — ambos métodos privados nuevos (`budgetObligationItems`, `privateMarketItems`) llaman a `$this->bucket($daysOverdue)`, el mismo método privado único ya existente.

    **2. Agregar a `tests/Feature/AccountsPayableReportTest.php`** (no eliminar ninguno de los 5 `it()` existentes ni la función `payableFixture()`):

    Imports nuevos a agregar al bloque `use` existente: `App\Enums\AccountNature`, `App\Enums\CashAccountType`, `App\Enums\PaymentMethod`, `App\Models\CashAccount`, `App\Models\ChartAccount`, `App\Models\WithholdingRule`, `App\Services\Accounting\PostExpenseVoucher`, `App\Services\Accounting\RegisterPayment` (mantener los imports ya existentes: `BudgetObligationStatus`, `PaymentOrderStatus`, `AccountsPayableReport`, `BudgetAppropriation`, `BudgetAvailabilityCertificate`, `BudgetObligation`, `BudgetRegistration`, `Company`, `Payment`, `PaymentOrder`, `ThirdParty`, `User`, `AccountsPayable`, `Livewire`).

    Agregar una función fixture nueva (nombre único en el namespace global de test helpers de Pest, sin colisionar con `payableFixture`/`accountingFixture`/`receivableFixture` de otros archivos):

    ```php
    function privateExpenseFixture(): array
    {
        $company = Company::factory()->create(['tax_id' => '900000005']);
        $thirdParty = ThirdParty::factory()->create(['company_id' => $company->id, 'tax_id' => '900373916', 'verification_digit' => 6]);

        $bank = ChartAccount::factory()->create(['company_id' => $company->id, 'code' => '111005', 'name' => 'Bancos', 'nature' => AccountNature::Debit]);
        $expenseAccount = ChartAccount::factory()->create(['company_id' => $company->id, 'code' => '513525', 'name' => 'Servicios', 'nature' => AccountNature::Debit]);
        $payableAccount = ChartAccount::factory()->credit()->create(['company_id' => $company->id, 'code' => '220505', 'name' => 'Proveedores']);
        $withholdingAccount = ChartAccount::factory()->credit()->create(['company_id' => $company->id, 'code' => '236540', 'name' => 'Retención']);
        $cashAccount = CashAccount::factory()->create(['company_id' => $company->id, 'chart_account_id' => $bank->id, 'type' => CashAccountType::Bank]);

        WithholdingRule::factory()->create([
            'company_id' => $company->id,
            'chart_account_id' => $withholdingAccount->id,
            'minimum_base' => 100000,
            'rate' => 4,
            'starts_on' => '2026-01-01',
        ]);

        return compact('company', 'thirdParty', 'expenseAccount', 'payableAccount', 'cashAccount');
    }
    ```

    Agregar los 3 tests nuevos (RED primero: correrlos y confirmar que fallan contra el `AccountsPayable.php` sin modificar; luego GREEN tras aplicar el cambio del paso 1):

    ```php
    it('includes a private-market expense record at its net-of-withholding pending amount', function () {
        $data = privateExpenseFixture();

        $voucher = app(PostExpenseVoucher::class)->handle($data['company'], $data['thirdParty'], [
            'third_party_id' => $data['thirdParty']->id,
            'expense_account_id' => $data['expenseAccount']->id,
            'payable_account_id' => $data['payableAccount']->id,
            'support_type' => 'Cuenta de cobro',
            'support_number' => 'CC-500',
            'accrual_date' => now()->toDateString(),
            'amount' => 200000,
            'has_valid_support' => true,
            'is_deductible' => true,
        ]);

        $rows = app(AccountsPayable::class)->openItems($data['company']);

        expect($rows)->toHaveCount(1)
            ->and($rows->first()['budget_obligation_id'])->toBeNull()
            ->and($rows->first()['number'])->toBe($voucher->number)
            ->and($rows->first()['amount'])->toBe(192000.0)
            ->and($rows->first()['pending'])->toBe(192000.0);
    });

    it('excludes a fully paid private-market expense record', function () {
        $data = privateExpenseFixture();

        $voucher = app(PostExpenseVoucher::class)->handle($data['company'], $data['thirdParty'], [
            'third_party_id' => $data['thirdParty']->id,
            'expense_account_id' => $data['expenseAccount']->id,
            'payable_account_id' => $data['payableAccount']->id,
            'support_type' => 'Cuenta de cobro',
            'support_number' => 'CC-501',
            'accrual_date' => now()->toDateString(),
            'amount' => 200000,
            'has_valid_support' => true,
            'is_deductible' => true,
        ]);

        app(RegisterPayment::class)->handle($data['company'], $data['cashAccount'], [
            'cash_account_id' => $data['cashAccount']->id,
            'counterparty_account_id' => $data['payableAccount']->id,
            'method' => PaymentMethod::Cash->value,
            'paid_on' => now()->toDateString(),
            'amount' => 192000,
        ], $voucher);

        $rows = app(AccountsPayable::class)->openItems($data['company']);

        expect($rows)->toHaveCount(0);
    });

    it('combines budget obligation and private-market expense rows for the same company', function () {
        $data = payableFixture();

        $expenseAccount = ChartAccount::factory()->create(['company_id' => $data['company']->id, 'code' => '513530', 'name' => 'Honorarios', 'nature' => AccountNature::Debit]);
        $payableAccount = ChartAccount::factory()->credit()->create(['company_id' => $data['company']->id, 'code' => '220510', 'name' => 'Proveedores mercado privado']);

        app(PostExpenseVoucher::class)->handle($data['company'], $data['thirdParty'], [
            'third_party_id' => $data['thirdParty']->id,
            'expense_account_id' => $expenseAccount->id,
            'payable_account_id' => $payableAccount->id,
            'support_type' => 'Cuenta de cobro',
            'support_number' => 'CC-600',
            'accrual_date' => now()->toDateString(),
            'amount' => 100000,
            'has_valid_support' => true,
            'is_deductible' => true,
        ]);

        $rows = app(AccountsPayable::class)->openItems($data['company']);

        expect($rows)->toHaveCount(2)
            ->and($rows->pluck('budget_obligation_id')->filter()->count())->toBe(1)
            ->and($rows->pluck('budget_obligation_id')->filter(fn ($id): bool => $id === null)->count())->toBe(1);
    });
    ```

    Nota de cálculo: con `minimum_base = 100000`, `rate = 4` (%) y `amount = 200000` (>= base), `ApplyWithholdingRules` retiene `200000 * 0.04 = 8000`; neto = `200000 - 8000 = 192000` — de ahí los valores `192000.0` en los asserts.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=AccountsPayableReportTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --compact --filter=AccountsPayableReportTest` sale con código 0, 11 tests pasando (8 existentes sin modificar — 5 de `openItems()` + 3 de página/CSV — más 3 nuevos)
    - `grep -c "private-market" tests/Feature/AccountsPayableReportTest.php` devuelve 3 o más
    - `grep -c "whereNull('budget_obligation_id')" app/Services/Accounting/AccountsPayable.php` devuelve 1
    - `grep -c "source_voucher_id" app/Services/Accounting/AccountsPayable.php` devuelve al menos 2 (el `whereIn` y el `groupBy`/`pluck`)
    - `grep -c "private function bucket" app/Services/Accounting/AccountsPayable.php` devuelve exactamente 1 (no duplicado)
    - `grep -c "payment_order_id" app/Services/Accounting/AccountsPayable.php` sigue devolviendo el mismo conteo que antes del cambio (lógica pública intacta, no tocada)
  </acceptance_criteria>
  <done>`AccountsPayable::openItems()` devuelve filas combinadas de `BudgetObligation` público y `ExpenseRecord` de mercado privado, con `number`/`amount` calculados por D-02/D-03 para las filas privadas; los 3 tests nuevos y los 5 existentes pasan.</done>
</task>

<task type="auto">
  <name>Task 2: Formateo, regresión de suite completa y cierre de AP-01</name>
  <files>ninguno nuevo (solo verificación; pint puede reformatear app/Services/Accounting/AccountsPayable.php si quedó estilo pendiente)</files>
  <read_first>
    app/Services/Accounting/AccountsPayable.php
    tests/Feature/AccountsPayableReportTest.php
    tests/Feature/AccountsReceivableReportTest.php
  </read_first>
  <action>
    1. Formatear el PHP tocado en Task 1:
    ```bash
    vendor/bin/pint --dirty --format agent
    ```

    2. Correr la suite completa de tests para confirmar cero regresiones (el gap de origen — Fase 5, Fase 3 — no debe verse afectado; en particular confirmar que `AccountsReceivableReportTest` y los tests de `WithholdingIcaMunicipalityTest`/`BankReconciliationTest`/`AccountingStatementsReportTest`/`GeneralLedgerReportTest` que usan `PostExpenseVoucher` siguen en verde, dado que ninguno de esos archivos se modificó):
    ```bash
    php artisan test --compact
    ```

    3. Confirmar en la salida que el conteo total de tests pasando es igual al baseline pre-plan (260 passed, según el registro de Phase 06 en `.planning/STATE.md`) más los 3 tests nuevos de Task 1 (263 passed esperado), sin ningún test fallido.
  </action>
  <verify>
    <automated>php artisan test --compact</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/pint --dirty --format agent` no reporta archivos con estilo pendiente
    - `php artisan test --compact` sale con código 0
    - La salida de `php artisan test --compact` no contiene la palabra "FAILED" ni "Failed"
    - El conteo de tests pasando es >= 263 (260 baseline + 3 nuevos de Task 1), sin tests nuevos fallando
  </acceptance_criteria>
  <done>Suite completa verde tras el cambio; código formateado; AP-01 queda listo para marcarse "Complete" en `.planning/REQUIREMENTS.md` por el orquestador tras esta ejecución.</done>
</task>

</tasks>

<verification>
1. `php artisan test --compact --filter=AccountsPayableReportTest` — 8/8 pasando (5 regresión + 3 nuevos de mercado privado).
2. `php artisan test --compact` (suite completa) — exit 0, sin fallos nuevos respecto al baseline de 260 (Phase 06).
3. `vendor/bin/pint --dirty --format agent` — limpio, sin archivos pendientes de formato.
4. Inspección manual de `app/Services/Accounting/AccountsPayable.php`: `openItems()` sigue siendo `public function openItems(Company $company): Collection` (firma sin cambios), y ahora delega a `budgetObligationItems()` + `privateMarketItems()`, con un único `bucket()` privado reusado por ambos.
5. `grep -n "AccountsPayable::class)->openItems" app/Http/Controllers/AccountingReportController.php` — confirma que el controlador NO se tocó (sigue llamando `openItems()` sin parámetros adicionales).
</verification>

<success_criteria>
- `AccountsPayable::openItems()` incluye `ExpenseRecord`s sin `BudgetObligation` asociada, no solo `BudgetObligation` (ROADMAP Phase 7, criterio 1).
- El reporte en pantalla, su CSV y su export Excel muestran las mismas filas de mercado privado sin lógica de consulta duplicada entre los tres — ninguno de esos 3 consumidores se modificó, todos siguen leyendo `accountsPayableRows()` -> `AccountsPayable::openItems()` (ROADMAP Phase 7, criterio 2).
- Un `ExpenseRecord` con retención ICA o ya conciliado aparece correctamente mientras esté pendiente de pago — cubierto por el test "includes a private-market expense record at its net-of-withholding pending amount", que ejercita `ApplyWithholdingRules` vía `PostExpenseVoucher` (ROADMAP Phase 7, criterio 3).
- La lógica de `BudgetObligation`/`payment_order_id` no se tocó (D-01) — GitHub Issue #3 queda como está, fuera de alcance.
- AP-01 queda cerrado.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cuentas-por-pagar-mercado-privado/07-01-SUMMARY.md`
</output>
