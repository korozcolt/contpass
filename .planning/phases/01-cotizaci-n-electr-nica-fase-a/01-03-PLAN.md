---
phase: 01-cotizaci-n-electr-nica-fase-a
plan: 3
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - app/Filament/Resources/Quotations/QuotationResource.php
  - app/Filament/Resources/Quotations/Schemas/QuotationForm.php
  - app/Filament/Resources/Quotations/Schemas/QuotationInfolist.php
  - app/Filament/Resources/Quotations/Pages/ListQuotations.php
  - app/Filament/Resources/Quotations/Pages/CreateQuotation.php
  - app/Filament/Resources/Quotations/Pages/EditQuotation.php
  - app/Filament/Resources/Quotations/Pages/ViewQuotation.php
  - app/Filament/Resources/Quotations/Tables/QuotationsTable.php
  - tests/Feature/QuotationLifecycleTest.php
autonomous: true
requirements: [QUOT-01, QUOT-03, QUOT-05, QUOT-06, QUOT-07]

must_haves:
  truths:
    - "Usuario puede crear una cotización para un tercero con una o más líneas, sin escribir el número manualmente (se genera solo)"
    - "Usuario puede transicionar Borrador -> Enviada -> Aceptada/Rechazada desde la tabla; Rechazar exige un motivo obligatorio"
    - "Usuario puede convertir una cotización Aceptada a ingreso en un clic; el botón desaparece una vez convertida"
    - "Una cotización convertida no se puede editar (403 en la ruta de edición) y su vista muestra un enlace al comprobante generado"
    - "Si la generación del número de cotización colisiona con otra (carrera de concurrencia contra el índice único compuesto company_id+number), el usuario ve el mensaje amigable del UI-SPEC en vez de un error crudo de base de datos"
  artifacts:
    - path: "app/Filament/Resources/Quotations/QuotationResource.php"
      provides: "recurso Filament con navigationGroup Operación, canEdit() que bloquea cotizaciones de solo lectura"
    - path: "app/Filament/Resources/Quotations/Tables/QuotationsTable.php"
      provides: "acciones de fila Enviar/Aceptar/Rechazar/Convertir a ingreso con colores/iconos de 01-UI-SPEC.md"
    - path: "app/Filament/Resources/Quotations/Schemas/QuotationInfolist.php"
      provides: "vista de solo lectura con enlace al Voucher resultante"
    - path: "app/Filament/Resources/Quotations/Pages/CreateQuotation.php"
      provides: "numeración automática vía BuildQuotationNumber::next() + captura de UniqueConstraintViolationException con el mensaje amigable de 01-UI-SPEC.md"
  key_links:
    - from: "app/Filament/Resources/Quotations/Pages/CreateQuotation.php"
      to: "app/Services/Accounting/BuildQuotationNumber.php"
      via: "mutateFormDataBeforeCreate asigna number vía BuildQuotationNumber::next()"
      pattern: "BuildQuotationNumber"
    - from: "app/Filament/Resources/Quotations/Pages/CreateQuotation.php"
      to: "database/migrations/*_create_quotations_table.php"
      via: "handleRecordCreation() captura UniqueConstraintViolationException del índice único compuesto company_id+number y la traduce a ValidationException con el copy exacto de 01-UI-SPEC.md"
      pattern: "UniqueConstraintViolationException"
    - from: "app/Filament/Resources/Quotations/Tables/QuotationsTable.php"
      to: "app/Services/Accounting/ConvertQuotationToIncome.php"
      via: "Action 'convert' invoca ->handle($record) dentro del closure de la acción"
      pattern: "ConvertQuotationToIncome"
    - from: "app/Filament/Resources/Quotations/QuotationResource.php"
      to: "app/Models/Quotation.php"
      via: "canEdit(Model $record) delega en $record->isReadOnly()"
      pattern: "isReadOnly"
---

<objective>
Construir el recurso Filament `QuotationResource` completo: formulario de creación/edición con líneas, ciclo de vida Borrador→Enviada→Aceptada/Rechazada por acciones de tabla, conversión a ingreso de un clic, y una vista de solo lectura con enlace al comprobante — todo sobre los modelos y servicios ya probados en los planes 1 y 2.

Purpose: Este es el único punto de contacto del usuario con la fase (QUOT-01, QUOT-03, QUOT-05, QUOT-06, QUOT-07) — el formulario nunca crea el número ni el comprobante directamente, delega en `BuildQuotationNumber` y `ConvertQuotationToIncome`.
Output: `QuotationResource` + `Schemas/QuotationForm` + `Schemas/QuotationInfolist` + `Tables/QuotationsTable` + 4 páginas (List/Create/Edit/View), y un test Pest Livewire que ejercita el flujo completo a través de la UI (no solo los servicios).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-CONTEXT.md
@.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-UI-SPEC.md
@.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-RESEARCH.md

<interfaces>
<!-- Contratos exactos consumidos en este plan, ya implementados en Plan 1 y Plan 2. NO releer ese código fuente. -->

From `app/Models/Quotation.php` (Plan 1):
```php
class Quotation extends Model
{
    // fillable: company_id, third_party_id, number, status, revenue_account_id,
    //           receivable_account_id, notes, expires_on, rejection_reason, voucher_id
    public function company(): BelongsTo;
    public function thirdParty(): BelongsTo;   // -> ThirdParty (name, tax_id, verification_digit)
    public function revenueAccount(): BelongsTo;
    public function receivableAccount(): BelongsTo;
    public function voucher(): BelongsTo;      // -> Voucher (nullable hasta convertir)
    public function lines(): HasMany;          // -> QuotationLine
    public function getTotalAttribute(): float;
    public function effectiveStatus(): App\Enums\QuotationStatus; // Draft/Sent/Accepted/Rejected/Converted/Expired
    public function isReadOnly(): bool;        // true solo si status === Converted
}
```

From `app/Models/QuotationLine.php` (Plan 1): fillable `quotation_id, description, quantity, unit_price, subtotal` — `subtotal` se autocalcula al guardar, no lo fijes manualmente desde el Repeater.

From `database/migrations/*_create_quotations_table.php` (Plan 1): `number` está protegido por un índice **único compuesto** `company_id+number` (no global) — bajo condiciones de carrera raras, dos usuarios pueden recibir el mismo `number` de `BuildQuotationNumber::next()` y el segundo `INSERT` falla con `Illuminate\Database\UniqueConstraintViolationException`. Este plan (Task 2) debe capturar esa excepción y mostrar el mensaje amigable de UI-SPEC, no dejar pasar el error crudo.

From `app/Services/Accounting/BuildQuotationNumber.php` (Plan 2): `public function next(Company $company): string` — retorna `COT-AAAA-NNNNN`. El `lockForUpdate()` interno es una optimización, no la garantía; la garantía es el índice único compuesto arriba.

From `app/Services/Accounting/ConvertQuotationToIncome.php` (Plan 2): `public function handle(Quotation $quotation): Voucher` — lanza `ValidationException` (clave `status`) si no es convertible; idempotente.

From `app/Filament/Support/AccountingFormFields.php` (existente, usar directamente, no reimplementar):
```php
class AccountingFormFields
{
    public static function companyId(): Hidden;
    public static function money(string $name, string $label): TextInput;
    public static function date(string $name, string $label): DatePicker;
    public static function thirdParty(string $name = 'third_party_id'): Select;
    public static function chartAccount(string $name, string $label, ?string $classPrefix = null): Select;
}
```

From `app/Filament/Resources/PaymentOrders/PaymentOrderResource.php` y `.../Tables/PaymentOrdersTable.php` (patrón exacto de acciones de fila con `->visible()` por estado + `->color()`/`->icon()` + `->form([...])` + `->action(function ($record, array $data) {...})`, replicar esta forma para las acciones de `QuotationsTable`).

From `app/Filament/Resources/Vouchers/VoucherResource.php`: ruta `'view' => ViewVoucher::route('/{record}')` — usar `App\Filament\Resources\Vouchers\VoucherResource::getUrl('view', ['record' => $quotation->voucher_id])` para el enlace al comprobante.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuotationResource + QuotationForm + QuotationInfolist</name>
  <files>app/Filament/Resources/Quotations/QuotationResource.php, app/Filament/Resources/Quotations/Schemas/QuotationForm.php, app/Filament/Resources/Quotations/Schemas/QuotationInfolist.php</files>
  <read_first>
    app/Filament/Resources/PaymentOrders/PaymentOrderResource.php
    app/Filament/Resources/PaymentOrders/Schemas/PaymentOrderForm.php
    app/Filament/Resources/Vouchers/Schemas/VoucherInfolist.php
    app/Filament/Support/AccountingFormFields.php
    app/Models/Quotation.php
  </read_first>
  <action>
    **`app/Filament/Resources/Quotations/QuotationResource.php`** — mismo esqueleto que `PaymentOrderResource`, con `canEdit()` sobrescrito para bloquear cotizaciones convertidas (QUOT-07, mismo principio de inmutabilidad que `Voucher::ensureEditable()`):
    ```php
    namespace App\Filament\Resources\Quotations;

    use App\Filament\Resources\Quotations\Pages\CreateQuotation;
    use App\Filament\Resources\Quotations\Pages\EditQuotation;
    use App\Filament\Resources\Quotations\Pages\ListQuotations;
    use App\Filament\Resources\Quotations\Pages\ViewQuotation;
    use App\Filament\Resources\Quotations\Schemas\QuotationForm;
    use App\Filament\Resources\Quotations\Schemas\QuotationInfolist;
    use App\Filament\Resources\Quotations\Tables\QuotationsTable;
    use App\Models\Quotation;
    use BackedEnum;
    use Filament\Resources\Resource;
    use Filament\Schemas\Schema;
    use Filament\Support\Icons\Heroicon;
    use Filament\Tables\Table;
    use Illuminate\Database\Eloquent\Model;

    class QuotationResource extends Resource
    {
        protected static ?string $model = Quotation::class;

        protected static string|BackedEnum|null $navigationIcon = Heroicon::DocumentCurrencyDollar;

        protected static ?string $navigationLabel = 'Cotizaciones';

        protected static ?string $modelLabel = 'cotización';

        protected static ?string $pluralModelLabel = 'cotizaciones';

        protected static string|\UnitEnum|null $navigationGroup = 'Operación';

        public static function canEdit(Model $record): bool
        {
            return ! $record->isReadOnly();
        }

        public static function form(Schema $schema): Schema
        {
            return QuotationForm::configure($schema);
        }

        public static function infolist(Schema $schema): Schema
        {
            return QuotationInfolist::configure($schema);
        }

        public static function table(Table $table): Table
        {
            return QuotationsTable::configure($table);
        }

        public static function getRelations(): array
        {
            return [];
        }

        public static function getPages(): array
        {
            return [
                'index' => ListQuotations::route('/'),
                'create' => CreateQuotation::route('/create'),
                'view' => ViewQuotation::route('/{record}'),
                'edit' => EditQuotation::route('/{record}/edit'),
            ];
        }
    }
    ```

    **`app/Filament/Resources/Quotations/Schemas/QuotationForm.php`** — el campo `number` NO aparece aquí (se genera en `CreateQuotation`, Task 2); `status` tampoco aparece (se muta solo por acciones de tabla, Task 3). Cuentas de clase 4/13 por D-05, líneas de texto libre por D-04, notas opcionales por D-02, vigencia +30 días por D-07:
    ```php
    namespace App\Filament\Resources\Quotations\Schemas;

    use App\Filament\Support\AccountingFormFields;
    use Filament\Forms\Components\Repeater;
    use Filament\Forms\Components\TextInput;
    use Filament\Forms\Components\Textarea;
    use Filament\Schemas\Schema;

    class QuotationForm
    {
        public static function configure(Schema $schema): Schema
        {
            return $schema
                ->columns(2)
                ->schema([
                    AccountingFormFields::companyId(),
                    AccountingFormFields::thirdParty(),
                    AccountingFormFields::chartAccount('revenue_account_id', 'Cuenta de ingreso', '4'),
                    AccountingFormFields::chartAccount('receivable_account_id', 'Cuenta por cobrar', '13'),
                    AccountingFormFields::date('expires_on', 'Vigencia hasta')->default(now()->addDays(30)),
                    Repeater::make('lines')
                        ->label('Líneas de la cotización')
                        ->relationship()
                        ->schema([
                            TextInput::make('description')->label('Descripción')->required()->maxLength(255)->columnSpan(2),
                            TextInput::make('quantity')->label('Cantidad')->numeric()->minValue(0.01)->step('0.01')->required(),
                            AccountingFormFields::money('unit_price', 'Valor unitario'),
                        ])
                        ->columns(4)
                        ->minItems(1)
                        ->required()
                        ->columnSpanFull(),
                    Textarea::make('notes')
                        ->label('Notas / términos comerciales')
                        ->helperText('Opcional. Se imprime en el PDF solo si tiene contenido (ej. "50% anticipo, 50% contra entrega").')
                        ->maxLength(1000)
                        ->columnSpanFull(),
                ]);
        }
    }
    ```
    Nota: `AccountingFormFields::date()` ya retorna `->required()`; encadenar `->default(now()->addDays(30))` sobreescribe el default de "hoy" sin romper el resto de la configuración (`DatePicker` acepta llamadas encadenadas adicionales).

    **`app/Filament/Resources/Quotations/Schemas/QuotationInfolist.php`** — vista de solo lectura (usada por `ViewQuotation`), con enlace al comprobante (QUOT-07) usando `TextEntry::url()`:
    ```php
    namespace App\Filament\Resources\Quotations\Schemas;

    use App\Filament\Resources\Vouchers\VoucherResource;
    use App\Models\Quotation;
    use Filament\Infolists\Components\RepeatableEntry;
    use Filament\Infolists\Components\TextEntry;
    use Filament\Schemas\Components\Section;
    use Filament\Schemas\Schema;

    class QuotationInfolist
    {
        public static function configure(Schema $schema): Schema
        {
            return $schema->components([
                Section::make('Cotización')->schema([
                    TextEntry::make('number')->label('Número'),
                    TextEntry::make('status')->label('Estado')->badge()
                        ->state(fn (Quotation $record) => $record->effectiveStatus()),
                    TextEntry::make('thirdParty.name')->label('Tercero'),
                    TextEntry::make('expires_on')->label('Vigencia hasta')->date(),
                    TextEntry::make('total')->label('Total')->money('COP'),
                    TextEntry::make('rejection_reason')->label('Motivo de rechazo')->placeholder('-'),
                    TextEntry::make('voucher.number')
                        ->label('Comprobante generado')
                        ->placeholder('Aún no convertida')
                        ->url(fn (Quotation $record): ?string => $record->voucher_id
                            ? VoucherResource::getUrl('view', ['record' => $record->voucher_id])
                            : null),
                ])->columns(3),
                Section::make('Líneas')->schema([
                    RepeatableEntry::make('lines')->label('')->schema([
                        TextEntry::make('description')->label('Descripción'),
                        TextEntry::make('quantity')->label('Cantidad'),
                        TextEntry::make('unit_price')->label('Valor unitario')->money('COP'),
                        TextEntry::make('subtotal')->label('Subtotal')->money('COP'),
                    ])->columns(4),
                ]),
            ]);
        }
    }
    ```
  </action>
  <verify>
    <automated>php artisan route:list --name=quotations 2>&1 | grep -q "quotations" && vendor/bin/pint --dirty --format agent</automated>
  </verify>
  <done>`QuotationResource`, `QuotationForm` y `QuotationInfolist` existen y compilan (rutas registradas); ningún campo `number`/`status` editable directamente en el formulario.</done>
  <acceptance_criteria>
    - `app/Filament/Resources/Quotations/QuotationResource.php` contiene `function canEdit(Model $record): bool` y `$record->isReadOnly()`
    - `app/Filament/Resources/Quotations/Schemas/QuotationForm.php` NO contiene `TextInput::make('number')` ni `Select::make('status')`
    - `app/Filament/Resources/Quotations/Schemas/QuotationInfolist.php` contiene `VoucherResource::getUrl('view'`
    - `vendor/bin/pint --dirty --format agent` termina sin cambios pendientes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Páginas List/Create/Edit/View (numeración automática + guard de solo lectura + mensaje amigable de colisión)</name>
  <files>app/Filament/Resources/Quotations/Pages/ListQuotations.php, app/Filament/Resources/Quotations/Pages/CreateQuotation.php, app/Filament/Resources/Quotations/Pages/EditQuotation.php, app/Filament/Resources/Quotations/Pages/ViewQuotation.php</files>
  <read_first>
    app/Filament/Resources/Vouchers/Pages/ListVouchers.php
    app/Filament/Resources/Vouchers/Pages/CreateVoucher.php
    app/Filament/Resources/Vouchers/Pages/EditVoucher.php
    app/Filament/Resources/Vouchers/Pages/ViewVoucher.php
    app/Services/Accounting/BuildQuotationNumber.php
    app/Services/Accounting/CurrentCompany.php
  </read_first>
  <action>
    **`ListQuotations.php`** — idéntico patrón a `ListVouchers`:
    ```php
    namespace App\Filament\Resources\Quotations\Pages;

    use App\Filament\Resources\Quotations\QuotationResource;
    use Filament\Actions\CreateAction;
    use Filament\Resources\Pages\ListRecords;

    class ListQuotations extends ListRecords
    {
        protected static string $resource = QuotationResource::class;

        protected function getHeaderActions(): array
        {
            return [
                CreateAction::make()->label('Nueva cotización'),
            ];
        }
    }
    ```

    **`CreateQuotation.php`** — asigna `number` (vía `BuildQuotationNumber`, QUOT-02) y `status = Draft` ANTES de crear, sin lógica de negocio adicional en la página (la numeración vive en el servicio, esto solo delega). Además, sobrescribe `handleRecordCreation()` para capturar la colisión rara del índice único compuesto `company_id+number` (Plan 1) y mostrarla como el mensaje amigable exacto de `01-UI-SPEC.md` — sección "Copywriting Contract", fila "Error state (numbering race / concurrency guard)" — en vez del error crudo de base de datos. Recordar (Plan 2, Task 1): `lockForUpdate()` dentro de `BuildQuotationNumber::next()` es solo una optimización; el índice único es la garantía real, y esta captura es lo que cierra el círculo hacia el usuario:
    ```php
    namespace App\Filament\Resources\Quotations\Pages;

    use App\Enums\QuotationStatus;
    use App\Filament\Resources\Quotations\QuotationResource;
    use App\Services\Accounting\BuildQuotationNumber;
    use App\Services\Accounting\CurrentCompany;
    use Filament\Resources\Pages\CreateRecord;
    use Illuminate\Database\Eloquent\Model;
    use Illuminate\Database\UniqueConstraintViolationException;
    use Illuminate\Validation\ValidationException;

    class CreateQuotation extends CreateRecord
    {
        protected static string $resource = QuotationResource::class;

        protected static bool $canCreateAnother = false;

        /**
         * @param  array<string, mixed>  $data
         * @return array<string, mixed>
         */
        protected function mutateFormDataBeforeCreate(array $data): array
        {
            $company = app(CurrentCompany::class)->get();

            $data['number'] = app(BuildQuotationNumber::class)->next($company);
            $data['status'] = QuotationStatus::Draft->value;

            return $data;
        }

        /**
         * @param  array<string, mixed>  $data
         */
        protected function handleRecordCreation(array $data): Model
        {
            try {
                return static::getModel()::create($data);
            } catch (UniqueConstraintViolationException) {
                throw ValidationException::withMessages([
                    'number' => 'No se pudo generar el número de cotización. Intenta nuevamente.',
                ]);
            }
        }
    }
    ```
    `Illuminate\Database\UniqueConstraintViolationException` es la excepción driver-agnóstica de Laravel para violaciones de restricción única (subclase de `QueryException`, normalizada por el framework para PostgreSQL/MySQL/SQLite) — no matchear por código SQLSTATE ni por texto del mensaje de error, usar el tipo directamente.

    **`EditQuotation.php`** — bloquea acceso directo a la ruta de edición de una cotización convertida (defensa en profundidad además de `QuotationResource::canEdit()`, que solo oculta el botón — ver Pitfall 4 de RESEARCH.md: "guardar incluso si la UI ya oculta el botón"):
    ```php
    namespace App\Filament\Resources\Quotations\Pages;

    use App\Filament\Resources\Quotations\QuotationResource;
    use Filament\Actions\DeleteAction;
    use Filament\Actions\ViewAction;
    use Filament\Resources\Pages\EditRecord;

    class EditQuotation extends EditRecord
    {
        protected static string $resource = QuotationResource::class;

        public function mount(int|string $record): void
        {
            parent::mount($record);

            abort_if($this->record->isReadOnly(), 403, 'Esta cotización ya fue convertida y no se puede editar.');
        }

        protected function getHeaderActions(): array
        {
            return [
                ViewAction::make(),
                DeleteAction::make(),
            ];
        }
    }
    ```

    **`ViewQuotation.php`**:
    ```php
    namespace App\Filament\Resources\Quotations\Pages;

    use App\Filament\Resources\Quotations\QuotationResource;
    use Filament\Actions\EditAction;
    use Filament\Resources\Pages\ViewRecord;

    class ViewQuotation extends ViewRecord
    {
        protected static string $resource = QuotationResource::class;

        protected function getHeaderActions(): array
        {
            return [
                EditAction::make(),
            ];
        }
    }
    ```
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent && php artisan route:list --name=quotations</automated>
  </verify>
  <done>Las 4 páginas existen; crear una cotización nunca pide `number` al usuario; abrir `/admin/quotations/{id}/edit` de una cotización convertida responde 403; una colisión de número a nivel de base de datos se traduce al mensaje amigable de UI-SPEC en vez de un error crudo (probado en Task 3).</done>
  <acceptance_criteria>
    - `app/Filament/Resources/Quotations/Pages/CreateQuotation.php` contiene `BuildQuotationNumber::class` y `QuotationStatus::Draft`
    - `app/Filament/Resources/Quotations/Pages/CreateQuotation.php` contiene `UniqueConstraintViolationException` y `'No se pudo generar el número de cotización. Intenta nuevamente.'`
    - `app/Filament/Resources/Quotations/Pages/EditQuotation.php` contiene `abort_if($this->record->isReadOnly(), 403`
    - `php artisan route:list --name=quotations` lista las 4 rutas (index/create/view/edit)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: QuotationsTable con ciclo de vida completo + test Livewire end-to-end</name>
  <files>app/Filament/Resources/Quotations/Tables/QuotationsTable.php, tests/Feature/QuotationLifecycleTest.php</files>
  <read_first>
    app/Filament/Resources/PaymentOrders/Tables/PaymentOrdersTable.php
    app/Filament/Resources/Vouchers/Tables/VouchersTable.php
    tests/Feature/WarehouseMovementTest.php
    app/Services/Accounting/ConvertQuotationToIncome.php
    app/Enums/QuotationStatus.php
  </read_first>
  <action>
    **`app/Filament/Resources/Quotations/Tables/QuotationsTable.php`** — columnas y las 4 acciones de fila exactas de `01-UI-SPEC.md` (colores/iconos/copy, tabla "Panel surface status colors"):
    ```php
    namespace App\Filament\Resources\Quotations\Tables;

    use App\Enums\QuotationStatus;
    use App\Models\Quotation;
    use App\Services\Accounting\ConvertQuotationToIncome;
    use Filament\Actions\Action;
    use Filament\Actions\EditAction;
    use Filament\Forms\Components\Textarea;
    use Filament\Notifications\Notification;
    use Filament\Support\Icons\Heroicon;
    use Filament\Tables\Columns\TextColumn;
    use Filament\Tables\Filters\SelectFilter;
    use Filament\Tables\Table;

    class QuotationsTable
    {
        public static function configure(Table $table): Table
        {
            return $table
                ->defaultSort('created_at', 'desc')
                ->columns([
                    TextColumn::make('number')->label('Número')->searchable()->sortable(),
                    TextColumn::make('thirdParty.name')->label('Tercero')->searchable(),
                    TextColumn::make('status')->label('Estado')->badge()
                        ->state(fn (Quotation $record) => $record->effectiveStatus()),
                    TextColumn::make('total')->label('Total')->money('COP')
                        ->state(fn (Quotation $record) => $record->total),
                    TextColumn::make('expires_on')->label('Vigencia hasta')->date()->sortable(),
                ])
                ->filters([
                    SelectFilter::make('status')->label('Estado')->options(QuotationStatus::class),
                ])
                ->emptyStateHeading('No hay cotizaciones')
                ->emptyStateDescription("Crea una cotización para un tercero desde el botón 'Nueva cotización'.")
                ->recordActions([
                    EditAction::make(),
                    Action::make('send')
                        ->label('Enviar')
                        ->icon(Heroicon::PaperAirplane)
                        ->color('primary')
                        ->visible(fn (Quotation $record): bool => $record->status === QuotationStatus::Draft)
                        ->action(function (Quotation $record): void {
                            $record->forceFill(['status' => QuotationStatus::Sent])->save();

                            Notification::make()->success()->title('Cotización enviada')->send();
                        }),
                    Action::make('accept')
                        ->label('Aceptar')
                        ->icon(Heroicon::CheckCircle)
                        ->color('success')
                        ->visible(fn (Quotation $record): bool => $record->effectiveStatus() === QuotationStatus::Sent)
                        ->action(function (Quotation $record): void {
                            $record->forceFill(['status' => QuotationStatus::Accepted])->save();

                            Notification::make()->success()->title('Cotización aceptada')->send();
                        }),
                    Action::make('reject')
                        ->label('Rechazar')
                        ->icon(Heroicon::XCircle)
                        ->color('danger')
                        ->visible(fn (Quotation $record): bool => $record->effectiveStatus() === QuotationStatus::Sent)
                        ->modalHeading(fn (Quotation $record): string => "Rechazar {$record->number}")
                        ->modalDescription('Vas a marcar esta cotización como Rechazada. Indica el motivo:')
                        ->form([
                            Textarea::make('rejection_reason')
                                ->label('Motivo de rechazo')
                                ->required()
                                ->validationMessages(['required' => 'Debes indicar un motivo de rechazo.'])
                                ->maxLength(255),
                        ])
                        ->action(function (Quotation $record, array $data): void {
                            $record->forceFill([
                                'status' => QuotationStatus::Rejected,
                                'rejection_reason' => $data['rejection_reason'],
                            ])->save();

                            Notification::make()->success()->title('Cotización rechazada')->send();
                        }),
                    Action::make('convert')
                        ->label('Convertir a ingreso')
                        ->icon(Heroicon::Banknotes)
                        ->color('success')
                        ->visible(fn (Quotation $record): bool => $record->status === QuotationStatus::Accepted && $record->voucher_id === null)
                        ->requiresConfirmation()
                        ->modalHeading('Convertir a ingreso')
                        ->modalDescription('Esta acción creará un comprobante de ingreso contable y no se puede deshacer. ¿Continuar?')
                        ->action(function (Quotation $record): void {
                            app(ConvertQuotationToIncome::class)->handle($record);

                            Notification::make()->success()->title('Cotización convertida a ingreso')->send();
                        }),
                ]);
        }
    }
    ```
    Nota sobre el bug de Filament v5 (`$get()` vs enum, ver issue #1 en `docs/roadmap-apolo.md`): este diseño evita el bug por construcción — `rejection_reason` es un campo `required()` estático dentro de un modal dedicado a la acción "Rechazar" (igual que `VouchersTable::adjust` y `PaymentOrdersTable::execute`), NO un campo condicional en el formulario principal que compare `$get('status')`. No introduzcas un campo condicional en `QuotationForm` para esto.

    **`tests/Feature/QuotationLifecycleTest.php`** — usa `Livewire::test()` + `Filament\Actions\Testing\TestAction` (mismo patrón que `tests/Feature/WarehouseMovementTest.php`) sobre `ListQuotations`, cubriendo el flujo completo end-to-end. Incluye además un test dedicado al mensaje amigable de colisión de número (issue de revisión: cierra el círculo del índice único compuesto de Plan 1 + la nota de diseño de Plan 2 + la captura de `CreateQuotation::handleRecordCreation()` de Task 2 de este mismo plan):
    ```php
    use App\Enums\QuotationStatus;
    use App\Filament\Resources\Quotations\Pages\CreateQuotation;
    use App\Filament\Resources\Quotations\Pages\EditQuotation;
    use App\Filament\Resources\Quotations\Pages\ListQuotations;
    use App\Models\ChartAccount;
    use App\Models\Company;
    use App\Models\Quotation;
    use App\Models\ThirdParty;
    use App\Models\User;
    use App\Services\Accounting\BuildQuotationNumber;
    use Filament\Actions\Testing\TestAction;
    use Illuminate\Enums\AccountNature; // usar App\Enums\AccountNature, ajustar import
    use Livewire\Livewire;

    function quotationFixture(): Quotation
    {
        $company = Company::factory()->create();
        $thirdParty = ThirdParty::factory()->create(['company_id' => $company->id]);
        $revenue = ChartAccount::factory()->credit()->create(['company_id' => $company->id, 'code' => '413595']);
        $receivable = ChartAccount::factory()->create(['company_id' => $company->id, 'code' => '130505']);

        $quotation = Quotation::factory()->create([
            'company_id' => $company->id,
            'third_party_id' => $thirdParty->id,
            'revenue_account_id' => $revenue->id,
            'receivable_account_id' => $receivable->id,
            'status' => QuotationStatus::Draft,
        ]);
        $quotation->lines()->create(['description' => 'Servicio', 'quantity' => 1, 'unit_price' => 100000]);

        return $quotation;
    }

    it('moves a quotation through the full lifecycle and converts it to income exactly once', function () {
        $this->actingAs(User::factory()->create());
        $quotation = quotationFixture();

        $component = Livewire::test(ListQuotations::class);

        $component->callAction(TestAction::make('send')->table($quotation));
        expect($quotation->fresh()->status)->toBe(QuotationStatus::Sent);

        $component->callAction(TestAction::make('accept')->table($quotation));
        expect($quotation->fresh()->status)->toBe(QuotationStatus::Accepted);

        $component->callAction(TestAction::make('convert')->table($quotation));

        $converted = $quotation->fresh();
        expect($converted->status)->toBe(QuotationStatus::Converted)
            ->and($converted->voucher_id)->not->toBeNull()
            ->and($converted->voucher->incomeRecord->amount)->toEqual('100000.00');

        $this->get(EditQuotation::getUrl(['record' => $converted]))->assertForbidden();
    });

    it('requires a rejection reason to reject a quotation', function () {
        $this->actingAs(User::factory()->create());
        $quotation = quotationFixture();
        $quotation->forceFill(['status' => QuotationStatus::Sent])->save();

        Livewire::test(ListQuotations::class)
            ->callAction(TestAction::make('reject')->table($quotation), ['rejection_reason' => ''])
            ->assertHasErrors(['rejection_reason' => 'required']);

        expect($quotation->fresh()->status)->toBe(QuotationStatus::Sent);
    });

    it('shows the UI-SPEC friendly message when the quotation number collides at insert time', function () {
        $this->actingAs(User::factory()->create());

        $company = Company::factory()->create();
        $thirdParty = ThirdParty::factory()->create(['company_id' => $company->id]);
        $revenue = ChartAccount::factory()->credit()->create(['company_id' => $company->id, 'code' => '413595']);
        $receivable = ChartAccount::factory()->create(['company_id' => $company->id, 'code' => '130505']);

        // Fuerza la colisión: una Quotation YA existe con el número que
        // BuildQuotationNumber::next() va a devolver de nuevo, porque
        // mockeamos el servicio para simular la ventana de carrera descrita
        // en 01-02-PLAN.md (lockForUpdate() no bloquea nada cuando count=0).
        $collidingNumber = 'COT-'.now()->format('Y').'-00001';

        Quotation::factory()->create([
            'company_id' => $company->id,
            'third_party_id' => $thirdParty->id,
            'revenue_account_id' => $revenue->id,
            'receivable_account_id' => $receivable->id,
            'number' => $collidingNumber,
        ]);

        $this->mock(BuildQuotationNumber::class, function ($mock) use ($collidingNumber): void {
            $mock->shouldReceive('next')->once()->andReturn($collidingNumber);
        });

        Livewire::test(CreateQuotation::class)
            ->fillForm([
                'company_id' => $company->id,
                'third_party_id' => $thirdParty->id,
                'revenue_account_id' => $revenue->id,
                'receivable_account_id' => $receivable->id,
                'expires_on' => now()->addDays(30)->toDateString(),
                'lines' => [
                    ['description' => 'Servicio', 'quantity' => 1, 'unit_price' => 100000],
                ],
            ])
            ->call('create')
            ->assertHasErrors(['number']);
    });
    ```
    Antes de finalizar, usa `search-docs` (Laravel Boost MCP) con las queries `["testing table actions", "TestAction table"]` para confirmar la firma exacta de `TestAction::make()->table()` y el método de aserción de errores de un action-form (`assertHasErrors` vs `assertHasTableActionErrors` — usa el que confirme la documentación de la versión instalada) antes de dar el test por definitivo; ajusta el nombre del método si la documentación indica uno distinto al usado arriba. Lo mismo aplica a `assertHasErrors(['number'])` sobre el formulario de creación (no una tabla) — confirma que `Livewire::test(CreateQuotation::class)->fillForm(...)->call('create')->assertHasErrors(...)` es el patrón correcto para páginas `CreateRecord` en esta versión de Filament antes de darlo por definitivo. Corrige también el import de `AccountNature` (debe ser `App\Enums\AccountNature`, el `Illuminate\Enums\AccountNature` del snippet es un placeholder a corregir).
  </action>
  <verify>
    <automated>php artisan test --compact --filter=QuotationLifecycleTest</automated>
  </verify>
  <done>Las 4 acciones de ciclo de vida existen con los colores/iconos/copy exactos de UI-SPEC; el test Livewire cubre Draft→Sent→Accepted→Converted con exactamente un Voucher creado, confirma que Rechazar exige motivo, que editar una cotización convertida devuelve 403, y que una colisión de número en la creación muestra el mensaje amigable de UI-SPEC.</done>
  <acceptance_criteria>
    - `php artisan test --compact --filter=QuotationLifecycleTest` sale con código 0
    - `app/Filament/Resources/Quotations/Tables/QuotationsTable.php` contiene las 4 acciones: `Action::make('send')`, `Action::make('accept')`, `Action::make('reject')`, `Action::make('convert')`
    - `app/Filament/Resources/Quotations/Tables/QuotationsTable.php` contiene `ConvertQuotationToIncome::class`
    - `app/Filament/Resources/Quotations/Tables/QuotationsTable.php` NO contiene ningún `$get('status')` (el guard evita el bug de Filament v5 por diseño, no por comparación de enum)
    - `tests/Feature/QuotationLifecycleTest.php` contiene un test que fuerza `BuildQuotationNumber` a devolver un número colisionado y verifica `assertHasErrors(['number'])`
  </acceptance_criteria>
</task>

</tasks>

<verification>
`php artisan test --compact --filter=Quotation` (cubre `QuotationModelTest`, `QuotationNumberingTest`, `QuotationConversionTest`, `QuotationLifecycleTest`) pasa completo. `vendor/bin/pint --dirty --format agent` limpio.
</verification>

<success_criteria>
Un usuario autenticado puede crear una cotización, moverla por su ciclo de vida completo desde la tabla, y convertirla a un `IncomeRecord` auditable en un clic — con el número generado automáticamente, la cotización convertida protegida contra edición directa, y una colisión rara de numeración mostrando el mensaje amigable del UI-SPEC en vez de un error crudo de base de datos.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cotizaci-n-electr-nica-fase-a/01-03-SUMMARY.md`
</output>
