---
phase: 06-limpieza-c-digo-hu-rfano-reteica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/WithholdingRuleController.php
  - app/Http/Requests/StoreWithholdingRuleRequest.php
  - resources/views/withholding-rules/index.blade.php
  - resources/views/withholding-rules/form.blade.php
  - .planning/codebase/STRUCTURE.md
  - .planning/phases/02-reteica-por-municipio-fase-c/deferred-items.md
autonomous: true
requirements: [TECHDEBT-01]

must_haves:
  truths:
    - "El controlador legacy WithholdingRuleController ya no existe en el repositorio"
    - "El form request legacy StoreWithholdingRuleRequest ya no existe en el repositorio"
    - "Las vistas blade legacy resources/views/withholding-rules/*.blade.php ya no existen en el repositorio"
    - "Ninguna ruta activa en routes/web.php o routes/console.php registraba WithholdingRuleController antes de eliminarlo (confirmado por auditoría grep antes de borrar, no asumido)"
    - "La suite de tests completa (php artisan test --compact) sigue pasando sin regresiones tras la eliminación"
  artifacts:
    - path: "app/Http/Controllers/WithholdingRuleController.php"
      provides: "Debe NO existir tras este plan (código huérfano eliminado, TECHDEBT-01)"
    - path: "app/Http/Requests/StoreWithholdingRuleRequest.php"
      provides: "Debe NO existir tras este plan (código huérfano eliminado, TECHDEBT-01)"
    - path: "resources/views/withholding-rules/index.blade.php"
      provides: "Debe NO existir tras este plan (código huérfano eliminado, TECHDEBT-01)"
    - path: "resources/views/withholding-rules/form.blade.php"
      provides: "Debe NO existir tras este plan (código huérfano eliminado, TECHDEBT-01)"
  key_links:
    - from: "routes/web.php"
      to: "WithholdingRuleController"
      via: "Route::... registration (debe NO existir, confirma que nunca fue una ruta viva)"
      pattern: "Route::[a-zA-Z]+\\([^)]*WithholdingRuleController"
    - from: "routes/console.php"
      to: "WithholdingRuleController"
      via: "Route::... registration (debe NO existir)"
      pattern: "WithholdingRuleController"
---

<objective>
Eliminar el código muerto sin ruta que quedó de la implementación pre-Filament de reglas de retención ICA (`WithholdingRuleController`, `StoreWithholdingRuleRequest`, vistas blade `withholding-rules/*`), cerrando el gap `RETICA-02-legacy-route` del audit v1.0. El `WithholdingRuleResource` de Filament (`app/Filament/Resources/WithholdingRules/`) es la implementación real y activa — permanece intacto.

Purpose: TECHDEBT-01 — eliminar una trampa latente para quien intente re-conectar este código huérfano, que además referencia la columna `concept` ya eliminada de `withholding_rules` en la migración de la Fase 2.
Output: 4 archivos legacy eliminados (+ directorio `resources/views/withholding-rules/` vacío eliminado), documentación de planning actualizada para reflejar la limpieza, suite de tests completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Verificado directamente contra el código del proyecto durante planning (2026-09-18). El executor debe usar esto sin re-explorar el codebase. -->

**Confirmación de que los 3 artefactos NO están ruteados (auditoría ya ejecutada durante planning):**

`routes/web.php` (archivo completo revisado) — no contiene ninguna línea `Route::` que registre `WithholdingRuleController`. Todas las rutas de este archivo son closures que invocan `AccountingReportController`/`QuotationPdfController`, o `Route::view('/', 'welcome')`. Cero referencias al controlador legacy.

`php artisan route:list 2>/dev/null | grep -i withholding` (ejecutado durante planning) devuelve solo 3 rutas, todas con nombre `filament.admin.resources.withholding-rules.*` (registradas automáticamente por Filament vía `WithholdingRuleResource`, NO por `routes/web.php`):
```
GET|HEAD admin/withholding-rules filament.admin.resources.withholding-rule...
GET|HEAD admin/withholding-rules/create filament.admin.resources.withholdi...
GET|HEAD admin/withholding-rules/{record}/edit filament.admin.resources.wi...
```
No existe ninguna ruta con nombre `withholding-rules.index` (sin el prefijo `filament.admin.resources.`) — el nombre de ruta que el propio controlador legacy usa internamente (`redirect()->route('withholding-rules.index')`) NUNCA resolvería si se ejecutara: es una prueba adicional de que este código está muerto, no solo sin acceso HTTP directo.

**Falsos positivos ya descartados durante planning (NO requieren cambios en este plan):**
- `tests/Feature/ExampleTest.php:48` contiene la ruta `/admin/withholding-rules` — es la ruta del `WithholdingRuleResource` de Filament (activa, cubierta arriba), no del controlador legacy. No tocar este archivo.
- `resources/views/components/layouts/app.blade.php:22` contiene la clave de array `'withholding-rules.index' => 'Retenciones'` dentro de un nav array de un layout Blade legacy más amplio (pre-Filament, cubre también `third-parties.index`, `chart-accounts.index`, etc. — ninguno de esos nombres de ruta existe tampoco). Este layout es código huérfano por su propio lado (fuera del alcance de TECHDEBT-01, que solo cubre los 3 artefactos de retención ICA) — NO tocar este archivo en este plan.
- `.planning/codebase/STRUCTURE.md:118` y `.planning/phases/02-reteica-por-municipio-fase-c/deferred-items.md` SÍ deben actualizarse (Task 2) porque son documentación de planning que queda desactualizada tras la eliminación, no código.

**Contenido completo de los 4 archivos a eliminar (referencia, no se necesita releer):**

`app/Http/Controllers/WithholdingRuleController.php` — 6 métodos REST (`index`, `create`, `store`, `show`, `edit`, `update`, `destroy`) que usan `view('withholding-rules.index')`/`view('withholding-rules.form')` y `redirect()->route('withholding-rules.index')` (ruta inexistente, ver arriba). Inyecta `CurrentCompany`.

`app/Http/Requests/StoreWithholdingRuleRequest.php` — `FormRequest` con regla `'concept' => ['required', 'string', 'max:255']`, columna eliminada por la migración de `02-02-PLAN.md`.

`resources/views/withholding-rules/index.blade.php` y `resources/views/withholding-rules/form.blade.php` — vistas Blade consumidas únicamente por el controlador anterior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditoría grep de seguridad pre-eliminación (bloqueante si aparece algo inesperado)</name>
  <files>ninguno (solo lectura/verificación, sin escritura)</files>
  <read_first>
    routes/web.php
    routes/console.php
    app/Http/Controllers/WithholdingRuleController.php
    app/Http/Requests/StoreWithholdingRuleRequest.php
  </read_first>
  <action>
    Ejecutar exactamente estos comandos y comparar la salida contra el resultado esperado documentado abajo. Si CUALQUIER comando produce una coincidencia fuera de lo esperado (es decir, una referencia viva desde código que SÍ se ejecuta — otro controlador, un service provider, una ruta activa, un job, un test que instancie la clase), detener el plan y reportarlo como bloqueante en el SUMMARY en vez de proceder al Task 2.

    1. `grep -rn "WithholdingRuleController" --include="*.php" app routes bootstrap config database tests`
       Esperado: solo la línea de declaración de la propia clase (`class WithholdingRuleController extends Controller`) en `app/Http/Controllers/WithholdingRuleController.php`. Cero apariciones en `routes/`, `bootstrap/`, `config/`, `database/`, `tests/`.

    2. `grep -rn "StoreWithholdingRuleRequest" --include="*.php" app routes bootstrap config database tests`
       Esperado: solo dentro de `app/Http/Requests/StoreWithholdingRuleRequest.php` (declaración) y `app/Http/Controllers/WithholdingRuleController.php` (import + firmas de `store()`/`update()`). Cero apariciones en cualquier otro archivo.

    3. `php artisan route:list --name=withholding-rules.index 2>/dev/null`
       Esperado: 0 filas (la ruta sin prefijo `filament.admin.resources.` no existe).

    4. `php artisan route:list 2>/dev/null | grep -i withholding`
       Esperado: exactamente 3 filas, todas con nombre `filament.admin.resources.withholding-rules.*` (index, create, {record}/edit) — ninguna apunta a `WithholdingRuleController`.

    5. `grep -rln "withholding-rules" resources/views tests --include="*.blade.php" --include="*.php"`
       Esperado: `resources/views/withholding-rules/index.blade.php`, `resources/views/withholding-rules/form.blade.php` (los 2 archivos a eliminar en Task 2), más `tests/Feature/ExampleTest.php` (ruta Filament `/admin/withholding-rules`, ver `<interfaces>` — no tocar) y `resources/views/components/layouts/app.blade.php` (nav array de layout legacy no relacionado, ver `<interfaces>` — no tocar).
  </action>
  <verify>
    <automated>grep -c "WithholdingRuleController" app/Http/Controllers/WithholdingRuleController.php</automated>
  </verify>
  <acceptance_criteria>
    - Comando 1 devuelve exactamente 1 línea de coincidencia (la declaración de clase), cero en `routes/web.php`/`routes/console.php`
    - Comando 2 devuelve coincidencias solo en `StoreWithholdingRuleRequest.php` y `WithholdingRuleController.php`, cero en otros archivos
    - Comando 3 devuelve 0 filas
    - Comando 4 devuelve exactamente 3 filas, todas con prefijo `filament.admin.resources.withholding-rules.`
    - Comando 5 devuelve exactamente 4 archivos: los 2 blade a eliminar + `ExampleTest.php` + `layouts/app.blade.php` (ambos ya explicados como falsos positivos en `<interfaces>`)
  </acceptance_criteria>
  <done>Auditoría grep completa; los 3 artefactos legacy están confirmados sin ninguna ruta ni referencia viva antes de proceder a eliminarlos en Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar los 4 archivos huérfanos, actualizar documentación de planning y verificar suite completa</name>
  <files>app/Http/Controllers/WithholdingRuleController.php, app/Http/Requests/StoreWithholdingRuleRequest.php, resources/views/withholding-rules/index.blade.php, resources/views/withholding-rules/form.blade.php, .planning/codebase/STRUCTURE.md, .planning/phases/02-reteica-por-municipio-fase-c/deferred-items.md</files>
  <read_first>
    app/Http/Controllers/WithholdingRuleController.php
    app/Http/Requests/StoreWithholdingRuleRequest.php
    resources/views/withholding-rules/index.blade.php
    resources/views/withholding-rules/form.blade.php
    .planning/codebase/STRUCTURE.md
    .planning/phases/02-reteica-por-municipio-fase-c/deferred-items.md
  </read_first>
  <action>
    1. Eliminar los 4 archivos y el directorio ahora vacío:
    ```bash
    rm app/Http/Controllers/WithholdingRuleController.php \
       app/Http/Requests/StoreWithholdingRuleRequest.php \
       resources/views/withholding-rules/index.blade.php \
       resources/views/withholding-rules/form.blade.php
    rmdir resources/views/withholding-rules
    ```

    2. Editar `.planning/codebase/STRUCTURE.md` línea 118: quitar el segmento `` `withholding-rules/`, `` de la lista de subdirectorios de `resources/views/` (el resto de la línea permanece igual — `accounting-reports/`, `payments/`, `income-records/`, etc. no cambian).

    3. Editar `.planning/phases/02-reteica-por-municipio-fase-c/deferred-items.md`: al final de la sección `## Orphaned legacy WithholdingRule controller/views (found during 02-02)`, agregar una línea nueva:
    ```
    - **Resolved:** Phase 06 (2026-09-18) eliminó los 4 archivos huérfanos (`WithholdingRuleController.php`, `StoreWithholdingRuleRequest.php`, `withholding-rules/index.blade.php`, `withholding-rules/form.blade.php`) tras confirmar por auditoría grep que ninguna ruta activa los referenciaba. Ver `06-01-SUMMARY.md`.
    ```
    No modificar ninguna otra línea de este archivo (es un registro histórico de la Fase 2, se preserva tal cual salvo esta nota de cierre).

    4. Formatear cualquier PHP tocado (no debería haber ninguno nuevo, solo eliminaciones — confirmar de todos modos):
    ```bash
    vendor/bin/pint --dirty --format agent
    ```

    5. Correr la suite completa de tests para confirmar cero regresiones:
    ```bash
    php artisan test --compact
    ```
  </action>
  <verify>
    <automated>php artisan test --compact</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/Http/Controllers/WithholdingRuleController.php` sale con código distinto de 0 (el archivo no existe)
    - `test -f app/Http/Requests/StoreWithholdingRuleRequest.php` sale con código distinto de 0
    - `test -d resources/views/withholding-rules` sale con código distinto de 0 (directorio eliminado, no solo vacío)
    - `grep -c "withholding-rules/" .planning/codebase/STRUCTURE.md` devuelve `0`
    - `grep -c "Resolved.*Phase 06" .planning/phases/02-reteica-por-municipio-fase-c/deferred-items.md` devuelve `1` o más
    - `php artisan route:list 2>/dev/null | grep -i withholding` sigue devolviendo exactamente las mismas 3 filas `filament.admin.resources.withholding-rules.*` que antes de este task (el `WithholdingRuleResource` de Filament permanece intacto y no se ve afectado)
    - `php artisan test --compact` sale con código 0, sin fallos nuevos respecto al baseline pre-plan (260 passed documentado en `.planning/v1.0-MILESTONE-AUDIT.md`)
    - `vendor/bin/pint --dirty --format agent` no reporta archivos con estilo pendiente
  </acceptance_criteria>
  <done>Los 4 archivos huérfanos y su directorio ya no existen en el repositorio; la documentación de planning refleja la limpieza; el `WithholdingRuleResource` de Filament sigue funcionando sin cambios; la suite completa de tests pasa sin regresiones.</done>
</task>

</tasks>

<verification>
1. `test -f app/Http/Controllers/WithholdingRuleController.php` — falla (no existe).
2. `test -f app/Http/Requests/StoreWithholdingRuleRequest.php` — falla (no existe).
3. `find resources/views/withholding-rules -maxdepth 0` — falla (directorio no existe).
4. `php artisan route:list 2>/dev/null | grep -i withholding` — exactamente 3 filas `filament.admin.resources.withholding-rules.*`, sin cambios respecto al estado previo (confirma que ninguna ruta activa dependía de los archivos eliminados).
5. `php artisan test --compact` — exit 0, mismo conteo de tests pasando que el baseline documentado en `.planning/v1.0-MILESTONE-AUDIT.md` (260 passed) menos cualquier test que hubiera cubierto directamente el código eliminado (ninguno, confirmado en Task 1: cero cobertura de test previa).
6. `vendor/bin/pint --dirty --format agent` — limpio.
</verification>

<success_criteria>
- `WithholdingRuleController`, `StoreWithholdingRuleRequest` y `resources/views/withholding-rules/*.blade.php` ya no existen en el repositorio.
- Se confirmó por auditoría grep (Task 1, ejecutada ANTES de eliminar) que ninguna ruta activa en `routes/web.php`/`routes/console.php` registraba estos archivos.
- `php artisan test --compact` sigue pasando sin regresiones tras la eliminación.
- TECHDEBT-01 queda cerrado.
</success_criteria>

<output>
After completion, create `.planning/phases/06-limpieza-c-digo-hu-rfano-reteica/06-01-SUMMARY.md`
</output>
